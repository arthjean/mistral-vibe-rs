CRITICAL: answer with text only. Do NOT call a tool. Any tool call is rejected.

This is a CONTEXT CHECKPOINT COMPACTION. Write a handoff another language model will read to resume this task.

Cover:
- The goal the operator is pursuing, with every constraint and preference stated
- The decisions already taken and why each was taken
- The files touched and where the work on them stands, one path with a one-line status
- What is left, stated as the next concrete step
- Every value, identifier or reference the next model needs to keep going

Stay short and structured. One line per file unless a snippet carries the meaning. Never restate what another line already says. Do not add a "Final Answer" section, because the handoff is the whole answer.

Put the ENTIRE handoff inside <summary></summary> tags and write nothing outside them:

<summary>
...the handoff goes here...
</summary>
