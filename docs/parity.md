# Parity Scorecard

Structural audit of Mistral Vibe RS against the upstream Python implementation.
Every axis carries a number, which is how to address one: axis 18 is hooks, axis
34 the conversation engine.

| Field | Value |
|---|---|
| First audit | 2026-08-04, Rust `5617d0c` |
| Last remeasure | 2026-08-19, in three passes: a whole-surface re-read of the reference, an audit of this document against both trees, then a distribution pass that built the instruments row 1 had none of and remeasured it from them. Eleven of the 33 existing rows moved, two more were amended without moving, and one row is new, the conversation engine, which no previous audit had scored. Five rows are restated up: slash commands 95 to 98 (`/retry` shipped 2026-08-08 and the row was never re-read), the CLI surface 90 to 92 (`vibe mcp remove` is implemented, so the row's claim was wrong), MCP 87 to 92 (`mcp/authUrl` is emitted), the system prompt 75 to 80 (`project_context` is implemented) and experiments 25 to 100 (the engine was ported 2026-08-17 and its oracle replays 502 of 502 with an empty ledger). Six are restated down: hooks 75 to 30, built-in tools 98 to 92 on two published result shapes, LLM backends 85 to 80 on the absent Vertex transport, the TUI 80 to 75 against what its harness actually covers, ACP 85 to 82 on two unserved commands, and agents 75 to 72 on the `task` result. Two are amended without moving: configuration names `show_greeting` as its one undeclared field, and sessions records the extra `format_version` key. The hooks row is the one the audit pass changed most and the reason it is worth doing: the first pass scored 55 on the stdin payload alone, and the audit found that no production path constructs a `HookManager` at all, so a configured hook is discovered, validated, counted and never run. The audit also ran every oracle with `--nocapture` and checked each printed counter this document quotes, all of which hold, and corrected the hook-payload row, which had undercounted the absent fields as five when they are six and had missed that three of the seven written fields are renamed rather than only recased. The open-divergences section below is new and holds what is not yet decided. Every other part carries the number its own remeasure produced, so the weighted total is a hand-maintained judgement over rows measured at different dates. The previous remeasure, 2026-08-12 from the telemetry oracle, restated telemetry and observability from 60 to 100 against 1 089 comparisons across 15 families, added the absent crash reporter and the unfilled `user_plan` to the accepted divergences; the one before it, 2026-08-11 from the autocompletion and voice oracles, restated those two from 70 and 65 to 100 against 159 and 1 065 comparisons; the one before that, 2026-08-10 from the two setup oracles, restated setup, onboarding and authentication from 35 to 100 against 182 and 491 comparisons. The third pass of 2026-08-19 is the distribution one and moved a single row: row 1 from 95 to 100, after the work that gave it something to measure. Before it, the installers defaulted to an owner `Cargo.toml` never declared, five matrix legs each overwrote the same one-line `SHA256SUMS`, the update notifier polled the reference's PyPI project and advertised a command that installs that project, the legacy `update_cache.json` was never read and a cache write replaced its whole section, the version was hand-copied into six files with nothing comparing them, `install.ps1` had no coverage on any platform and the completion files could drift from clap silently. Each of those now has an artifact that fails, three new update-cache traces replay 25 events over the on-disk layout, and the four things this port answers differently are ledger rows rather than unrecorded judgement. What the pass did not produce is a published release: US-218 was cancelled the same day, so the release workflow and the resolved asset URLs are asserted and not observed, which the row says in place |
| Python reference | `b78b451`, reference package version 2.24.0, against this port's `[workspace.package] version` 2.24.0 |
| Reference checkout | `/home/arthur/dev/mistral-vibe` on Linux and `C:\dev\mistral-vibe` on Windows, relocatable through `VIBE_REFERENCE`, resolved by `vibe_core::parity::reference_root`. Every path in the Reference column of the parity table and in the two divergence tables is relative to that root and was resolved at the pin before being written here |
| Pin sources | `vibe_core::parity::REFERENCE_COMMIT` and `EXPECTED_COMMIT` in `scripts/parity/pin.py`, held equal by `crates/vibe-core/src/parity/parity_tests.rs` |
| Restore an off-pin checkout | `git -C /home/arthur/dev/mistral-vibe checkout b78b451c39eab9213393ad2f45908e8562a5c5e7` |
| Weighted score against the pin | **86.6/100**, from 5 628 over a weight of 65 (was 86.5 before row 1 was remeasured, 83 at the remeasure before that, 65 at first audit). The move is one row: distribution 95 to 100 at weight 1, so the raw sum rises 5 623 + 5 = 5 628 and 5 628 / 65 = 86.58, stated as 86.6. The weighting is stated rather than judged, so the number can be recomputed from the table: weight 3 for a contract a session exercises continuously, which is axes 3, 6, 14, 16, 17, 30 and 34; weight 1 for a peripheral one, axes 1, 5, 21, 22, 23, 24, 25, 27, 32 and 33; weight 2 for the remaining 17. The rise over 83 is almost entirely experiments, which moved 75 raw points on a part no oracle had read since it shipped, offset by hooks losing 45 |
| Weighted score against upstream `HEAD` | **roughly 78/100** at reference `5e6aa0f`, package version 2.24.2. Carried at lower precision than the pinned score on purpose: no oracle measures it, because every corpus was captured at the pin. It is the pinned score with the axes the drift section names marked down, the app-server protocol hardest, and it exists to keep one fact visible: the pin is two releases behind, so the first column is parity with a target rather than with a product |

## Two targets

Every score in this document answers one of two questions, and they no longer
give the same answer.

**Against the pin** is what the oracles measure. `b78b451` is the commit every
committed corpus was captured from, so a replay is only an oracle for that
revision. This is the number that moves when work lands here.

**Against upstream `HEAD`** is what a user comparing the two products
experiences. The pin is v2.24.0 and upstream is v2.24.2, which is 201 source
files and roughly +13 900 / -3 700 lines apart, measured with `git diff --stat
b78b451..HEAD -- vibe/` in the reference checkout. Some of that drift is
renaming this port's whole session surface out from under it.

Keeping both numbers is deliberate. A port that reaches 100 against a pin it
never advances has not reached parity with anything a user runs. Re-pinning is
the only way to close the second column, and `AGENTS.md` requires regenerating
every committed corpus in the same change, so the cost is real and the decision
belongs in the open rather than inside a single score.

## Method

Scores measure **reproduced surface**, not behavioral conformance. They come from
diffing inventories, not from running both implementations side by side:

- app-server JSON-RPC method names (91 upstream `SERVER_METHODS` plus 7 `clientTool/*`, counted from the pinned checkout by `scripts/parity/app_server_surface.py`);
- user-facing configuration keys (`vibe/core/config/vibe_schema.py` against
  `LayeredConfig::schema()`);
- published agent tool names and schemas;
- slash commands and CLI flags;
- module presence and depth, measured in lines of production code.

A part scores 100 when its externally observable contract is fully reproduced.
Anything measured only by module presence carries more uncertainty than anything
measured by name-level diff, and the table notes which is which.

Module presence is the weakest evidence there is, and the 2026-08-19 audit
showed why: every subsystem in this port was checked for whether anything
outside its own module and outside test code constructs it, and hooks was the
one that nothing does. A row that says a mechanism is reproduced now means it
runs, not that it compiles. Rows carrying **Measured by a differential oracle**
are replayed against a committed corpus; the others are inventory diffs or
structural judgements, and say so in place.

Every row of the parity table names the reference tree it was measured against,
so a reader can open the same files the score came from. Those paths are
relative to the checkout root recorded in the header, they are the state of the
reference at the pin, and the two the drift section names as moved say so in
place. Citing them rather than quoting them is what `NOTICE` requires.

Where a differential oracle exists, the score comes from running it rather than
from reading source, and the row says so and carries the command that reproduces
it. Twenty-one modules matching `*_parity_tests.rs` hold those replays, against
corpora captured by the eighteen capture scripts under `scripts/parity/`. Every
one of them replays unconditionally; only the probe that recaptures from the
pinned checkout skips, so a missing or off-pin reference never fails
`cargo test`. A corpus is committed on the terms `NOTICE` sets: names, pointers,
verdicts and counts, with every reference-authored sentence reduced to a length
and a SHA-256. Each replay also carries a ledger of what is still divergent, and
fails both on a divergence no entry names and on an entry whose divergence has
stopped reproducing, which is what keeps a score from drifting away from the
code under it.

### Measured volumes

| Tree | Lines |
|---|---|
| `vibe/` at the pin (Python, tests live in `tests/`) | 92 207 across 475 files |
| `crates/` total | 218 911 |
| `crates/` production, inline `#[cfg(test)]` blocks removed | roughly 124 200 |

Remeasured 2026-08-19. Volume is context, never a score: the port carries more
lines than the reference in every layer, and the parts it scores lowest are not
the thin ones.

## Parity by part

| # | Part | Score | Reference | State and gaps |
|---|---|---|---|---|
| 1 | Distribution, updates, installers | 100 | `vibe/cli/update_notifier/`, `vibe/utils/cache_store.py`, `scripts/bump_version.py`, `pyinstaller/`, `vibe.spec`, `vibe-acp.spec`, `vibe-app-server.spec`, `action.yml` | Restated 2026-08-19 from 95, which was the one number in this document that rested on no measurement at all: the installers defaulted to an owner the repository never had, each matrix leg overwrote the previous leg's `SHA256SUMS`, and the notifier polled the reference's own PyPI project. Every claim below now names what fails if it stops holding. **Partly measured by a differential oracle**: three update-cache traces in `crates/vibe-cli/tests/runtime-parity/terminal-services.json` replay 25 events over the on-disk layout, ten deterministic runs each, through `corpus_replays_update_attention_narration_and_exit_behavior` in `crates/vibe-cli/src/tui/runtime_parity_tests/terminal_services.rs`; `update-cache-migration` and `update-cache-merge` declare no divergence, so the legacy `update_cache.json` read and the section merge are conformant rather than asserted, and `update-cache-faults` declares six, each a refusal this port answers where the reference raises or parses without a ceiling. The `update-gateway` trace still replays its 13 events, because the PyPI adapter is retained rather than removed; what changed is which adapter production resolves, which is the ledger row below. The version literal is written once: `[workspace.package] version` is 2.24.0, matching `vibe_core::parity::REFERENCE_VERSION`, and the five files that copy it are held to it by `every_hand_written_version_matches_the_workspace_manifest` and `no_release_script_or_workflow_hides_another_version_literal`, which fail both on a copy that disagrees and on a carrier that stops carrying it. Both installers resolve their release base from `[package] repository` with no literal owner slug, bound by `both_installers_fetch_from_the_declared_repository`, and one aggregate `SHA256SUMS` accumulates every packaged target: `crates/vibe-cli/tests/installer_scripts.rs` runs the real `scripts/ci/package-release.sh` and holds the manifest sorted with no duplicate line across targets and across a repackage of the same one, and the collection job that assembles it is held to the same shape by `the_release_workflow_collects_one_aggregate_manifest`. Both installers are now exercised on their native platform, and unevenly: `install.sh` is executed by `cargo test` itself, which installs from a `file://` base with a shimmed `curl` and asserts the refusals for a missing digest line and an `http://` base, and again by `.github/workflows/action.yml`; `install.ps1` is executed only by the `windows-installer` job of `.github/workflows/ci.yml`, whose runner, packaging step, driver script and 20-minute budget `the_powershell_installer_is_exercised_on_a_windows_runner` asserts. That job is committed and asserted and has not yet run on a `windows-2022` runner, so the Windows build and its wall clock are asserted rather than observed, which is the one claim in this row no artifact here can close. The four completion files are held to clap's non-hidden long flags and visible aliases by `every_completion_file_offers_exactly_the_flags_clap_declares`, and the published binary set is held to two by `the_release_publishes_exactly_the_two_binaries_the_ledger_records`. What this port answers differently is decided rather than open: four rows in the accepted-divergence table below cover the absent standalone `vibe-app-server` command, the compiled-in release notes, the production gateway resolution and the upgrade command list. Publication is deferred, not missing: `.github/workflows/release.yml` is committed and its shape asserted, no `v*` tag has been pushed, and the score measures reproduced surface as the Method section defines it, not a published artifact. Still exceeds upstream packaging on the transactional `.new`/`.previous` staging and rollback the reference has no counterpart for |
| 2 | Slash commands | 98 | `vibe/cli/commands.py`, `vibe/cli/autocompletion/slash_command.py` | Restated 2026-08-19: `/retry` shipped 2026-08-08 and the row was never remeasured. 31 of 31 aliases and 28 of 28 command keys are identical, diffed against `vibe/cli/commands.py` at the pin, including the two aliases that ride on another key, `/connectors` on `mcp` and `/continue` on `resume`. Measured by inventory diff rather than by oracle, which is why the row is not 100 |
| 3 | Built-in tools | 92 | `vibe/core/tools/builtins/`, `vibe/core/tools/builtins/prompts/`, `vibe/questions.py`, `vibe/core/subagents.py` | **Measured by two differential oracles**. Surface: 12/12 names and 12/12 schemas on the base surface, 16/16 under the managed shell rollout, 10/10 on the Windows families, 38/38 against the committed digest, and 92/92 argument fixtures now returning the reference verdict. Execution: 35/41 cases match the reference over the committed fixture tree, and the 6 others diverge only in the wording of a warning or an applied-edit message, which `NOTICE` keeps out of reach. `grep` runs on the ripgrep library crates with smart case, the ignore files and the 23 configured exclusion globs; `read_file`, `write_file`, `edit` and `todo` publish the reference result fields and the field-per-line rendering the agent loop sends. Descriptions and message text are compared for presence only, never text, as `NOTICE` requires. Restated 2026-08-19, down from 98: the execution oracle covers `read_file`, `grep`, `write_file`, `edit` and `todo`, and two published results outside its reach diverge. `web_fetch` answers `contentType` and `wasTruncated` where `vibe/core/tools/builtins/web_fetch.py` publishes `content_type` and `was_truncated`, and `task` answers a delegation effect where `vibe/core/subagents.py` publishes `response`, `turns_used` and `completed`. Both are field-per-line rendered into the model's transcript, so both are observable. Recorded under open divergences |
| 4 | Tool infrastructure (registry, schemas, filtering) | 95 | `vibe/core/tools/base.py`, `vibe/core/tools/manager.py`, `vibe/core/tools/utils.py`, `vibe/core/tools/permissions.py`, `vibe/core/tools/ui.py` | `object_schema` removed, `apply_defaults` applies schema defaults, `validate_arguments` understands `$ref`, `anyOf`, `items` and array-form `type`. `coerce_and_validate` reproduces the reference Pydantic lax coercion before dispatch, so a handler reads the coerced value, proven by 92/92 replayed fixtures with 0 wrongly accepted and 0 stricter. **Per-tool configuration is measured by a third oracle**: 26/26 tool classes, 22/22 keys and 146/146 `(tool, key)` pairs declared and read, replayed against `tests/tool-config/defaults.json`. `matching.rs` matches globs, `re:` prefixes and is case-insensitive |
| 5 | Worktree (`--worktree`) | 90 | `vibe/core/worktree.py`, moved to `vibe/core/git/worktree/` and `vibe/core/git/repo.py` at v2.24.2 | `startup/worktree.rs`, full create/reuse/cleanup/branch lifecycle |
| 6 | Managed shell and terminals | 92 | `vibe/core/tools/builtins/managed_shell/`, `vibe/core/tools/builtins/experimental_bash.py`, `vibe/core/tools/builtins/windows_shell.py`, `vibe/core/tools/builtins/git_bash.py`, `vibe/core/tools/builtins/bash.py`, `vibe/core/tools/terminal_runtime.py` | `TerminalManager` plus the reference shell policy, and the `bash_*`, `git_bash` and `powershell` families now publish conformant schemas. **The policy is measured by a differential oracle**: 28/28 grammar extractions, 45/45 path-inspecting commands with 0 missing and 0 invented, 23/23 escaping-operand cases and 60/63 resolutions match the reference, the 3 others being ledgered places this port asks where the reference grants. Reproduce with `cargo test -p vibe-core --all-features shell_parity_tests -- --nocapture`. A managed session now runs under a real PTY on POSIX, so a program that probes for a terminal finds one, a control key written through `<family>_stdin` reaches the foreground process group, and a hard timeout terminates the whole group including a grandchild that outlived its parent. Each session writes a manifest beside its log, and one left behind by a previous process is listed, read and inspected as `orphaned`. Residual: the session behavior is asserted by named tests rather than by a differential oracle, the Windows families still execute on pipes, and a host that provides no PTY backend falls back to pipes with the reduced capability reported as a null `ptyBackend` |
| 7 | CLI surface (flags, modes) | 92 | `vibe/cli/entrypoint.py`, `vibe/cli/cli.py`, `vibe/cli/mcp_command.py` | Every upstream flag present, and tool filtering matches globs and `re:` prefixes. Restated 2026-08-19: `vibe mcp remove` is implemented in `crates/vibe-cli/src/mcp_command.rs`, so the row's previous claim that the whole subcommand is missing was wrong. What is missing is `vibe mcp add` and its 12 flags, which `vibe/cli/mcp_command.py` serves and this port answers with a pointer to `/mcp add` |
| 8 | ACP (`vibe-acp`) | 82 | `vibe/acp/`, `vibe/acp/commands/`, `vibe/acp/tools/` | Agent, sessions, updates, teleport, mcp, proxy. Comparable volume. Restated 2026-08-19, down from 85: the adapter serves 8 of the 10 commands `vibe/acp/commands/registry.py` advertises. `leanstall` and `unleanstall` exist in the CLI registry and not in `crates/vibe-acp/src/commands.rs`, so an editor client is offered a smaller set than a terminal one. Recorded under open divergences |
| 9 | Rewind | 95 | `vibe/core/rewind/`, `vibe/app_server/_projection.py` | Both methods take `entryId` and answer the declared shapes, validated against the app-server census. The restore plan comes from the checkpoint log rather than from a parallel snapshot store, so a partial write reports one error per path and still keeps what landed, and a message-list reset that is not a rewind clears the log and reopens a running turn. Residual: the TUI confirms in one step where the reference confirms in two, recorded in `crates/vibe-cli/tests/runtime-parity/session-management.json` |
| 10 | LLM backends | 80 | `vibe/core/llm/`, `vibe/core/llm/backend/` | 6 styles (mistral, openai, reasoning, openai-responses, anthropic, vertex-anthropic), SSE streaming, retry. Image, cache and tool-id adaptation details unverified. Restated 2026-08-19, down from 85: `vertex-anthropic` is a recognized style that routes to the Anthropic body and header shape, with none of the transport `vibe/core/llm/backend/vertex.py` builds. Application Default Credentials, the token refresh, the regional `{region}-aiplatform.googleapis.com` base and the `rawPredict` and `streamRawPredict` endpoints are all absent, so the style resolves and cannot reach Vertex. Recorded under open divergences |
| 11 | MCP | 92 | `vibe/core/tools/mcp/`, `vibe/core/auth/mcp_oauth.py`, `vibe/core/config/mcp_servers.py`, `vibe/cli/mcp_command.py` | stdio, streamable-http, OAuth, registry, toggle. Tools now published as `{alias}_{tool}` matching upstream. Sampling is served on both transports: an entry that enables it advertises the capability at `initialize` and its `sampling/createMessage` requests are answered by the provider the turn itself runs on, with the system prompt prepended, an unknown role read as assistant, non-text blocks skipped, and a backend failure returned as a structured error rather than as a partial completion; an entry that disables it advertises nothing and refuses the request with the capability-absent error. Missing `mcp/authUrl` and the `vibe mcp` CLI. Restated 2026-08-19: `mcp/authUrl` is emitted, from `crates/vibe-app-server/src/resources.rs`, so the row's previous claim that it is missing was stale. `vibe mcp add` remains the one gap, recorded on the CLI row |
| 12 | Trusted folders and permissions | 92 | `vibe/core/trusted_folders.py`, `vibe/permissions.py`, `vibe/core/tools/permissions.py` | **Partly measured by differential oracle**: the 4 reference permission scopes are spoken with 0 missing and 0 invented, the requirement carries exactly `scope`, `invocationPattern`, `sessionPattern` and `label`, the 138-entry arity table matches entry for entry, and 20 session-pattern and 19 wildcard cases replay the reference verdict. Reproduce with `cargo test -p vibe-core --all-features permission_parity_tests -- --nocapture`. `policy.rs` keeps this port's own trust roots, leases and atomic revocation on top, which the reference expresses as project roots. The shell policy now resolves from the four reference lists and is measured by its own oracle, recorded under managed shell |
| 13 | Programmatic mode (`-p`) | 85 | `vibe/cli/programmatic.py`, `vibe/cli/entrypoint.py` | text / json / streaming all implemented |
| 14 | TUI (composer, transcript, pickers) | 75 | `vibe/cli/textual_ui/`, `vibe/cli/textual_ui/widgets/`, `vibe/cli/textual_ui/screens/`, `vibe/cli/textual_ui/app.py` | Broad coverage, backed by a dedicated observable-parity harness (JSON traces plus Python oracles). Missing vim navigation, word selection, `load_more`, braille rendering. Restated 2026-08-19, down from 80, against what the harness actually covers: 38 traces across six fixtures in `crates/vibe-cli/tests/runtime-parity/`, with 11 recorded unavailable, measured against roughly 20 000 lines of `vibe/cli/textual_ui/`. `vibe/cli/textual_ui/word_selection.py`, `vibe/cli/textual_ui/widgets/vim_navigation.py`, `vibe/cli/textual_ui/widgets/braille_renderer.py`, `vibe/cli/textual_ui/widgets/load_more.py` and `vibe/cli/textual_ui/widgets/feedback_bar.py` have no counterpart |
| 15 | Review and turn diff | 95 | `vibe/core/review/`, `vibe/app_server/review.py` | **Measured by the checkpoint oracle plus the app-server census**: the six methods answer from the session's checkpoint log rather than from a map production never wrote to, and 73/73 replayed scenarios conform on regions, scopes, projections and anchors. All seven decision targets resolve, a revert is written to disk and rolled back with the log when the write does not land, and a hand edit keeps its own owner slot. Reproduce with `cargo test -p vibe-core --all-features checkpoint_parity_tests -- --nocapture`. Residual: the four read methods are census-validated from a bare probe and from a written session, and the two mutations answer an empty object the census cannot discriminate |
| 16 | Sessions, resume, fork, history | 90 | `vibe/core/session/`, `vibe/core/types.py` | `storage.rs`: metadata, pagination, migration, file locks, handoff journal. The two rewind methods now address a stable entry identity and both validate against the app-server census from a written session, with `SessionRewindResponse` carrying all five declared fields. Residual: `history/list` still answers a flat stored-message list where the reference answers a `PublicHistoryPage`, which is why a bare probe cannot reach it. Amended 2026-08-19: the on-disk layout conforms, `meta.json` beside `messages.jsonl` under the same names, and the serde renames bring `session_id`, `experiments`, `total_messages` and `stats` back to what `vibe/core/session/session_logger.py` writes. `format_version` is an extra key this port writes that the reference never does; the reference model ignores unknown keys, so a file written here still loads there |
| 17 | app-server protocol | 93 | `vibe/app_server/`, `vibe/app_server/protocol.py`, `vibe/app_server/models.py` | **Measured by differential oracle**, restated at the `b78b451` re-pin: 91 of 91 reference methods declared and 89 of 91 routed with 0 invented names inside the inventory, 15 of 15 notifications emitted with 0 invented, 7 of 7 `clientTool/*` issued, 12 of 12 error codes spoken, 19 of 22 enum vocabularies compared and 373 models in the census. 21 of the 23 methods a bare probe session can reach validate against the census with 0 missing required and 0 surplus aliases. Reproduce with `cargo test -p vibe-app-server --all-features app_server_surface_parity_tests -- --nocapture`. The nine `projectLinks/*` answers are validated against the same census from a repository fixture, since a bare probe reaches only their ineligible form. The two `session/rewind*` answers are validated the same way, from a session a test writes and drives a turn against. Residual: v2.24.0 added `identity/read` and `workspace/worktrees/list`, declared here and unrouted, gave `ConfigView` the three unpinned-model fields `config/read` and `runtime/read` therefore no longer carry, and made `PublicRetryCategory` reachable; `TurnStartParams.injected` is carried on the wire and defaults false, with the first path that sets it arriving with the compaction envelope; `TerminalEmulator` stays unmodeled, and 58 responses need a backend or a written session neither harness stands up, so they are declared and routed but not census-validated. `account/read` answers with the reference field names and reaches three of its four statuses; the fourth, `unauthorized`, is a console verdict this port has no client for, restated as its own divergence below rather than closed by the setup parity work |
| 18 | Hooks | 30 | `vibe/core/hooks/`, `vibe/core/agent_loop_hooks.py` | Restated twice on 2026-08-19, from 75 to 55 on the stdin payload and then to 30 on wiring, the second being the finding that matters: **no production path runs a hook**. `HookManager` is constructed in exactly three places, all inside the `#[cfg(test)]` block of `crates/vibe-core/src/extensions.rs`, and nothing outside that module calls its chain. What this port does with `hooks.toml` is discover it, validate its entries and count them into `hooks_count`, which is what `crates/vibe-app-server/src/workspace/agents.rs` publishes; an operator who configures a hook sees it counted and never sees it run. The chain itself is written and unit-tested: the event types, the matcher, the timeout, the retry ladder, the strict stance and the response contract are all reproduced against `vibe/core/hooks/`, and reading the hook's answer is conformant. What is not reproduced is the invocation written to the hook's stdin, which is the second divergence and would matter the moment the first is fixed. `vibe/core/hooks/executor.py` writes `invocation.model_dump_json()`, so a hook reads the snake_case fields `vibe/core/hooks/models.py` declares: `hook_event_name`, `session_id`, `transcript_path`, `cwd`, `parent_session_id`, `tool_name`, `tool_call_id`, `tool_input`, `tool_status`, `tool_output`, `tool_output_text`, `tool_error` and `duration_ms`. This port writes seven fields in camelCase: `sessionId`, `parentSessionId`, `toolName` and `toolCallId` keep their name and change case, while `hook_event_name` becomes `kind` (carrying the same `pre_tool`, `post_tool` and `post_agent` values), `tool_input` becomes `payload` and `tool_output_text` becomes `outputText`. Six reference fields have no counterpart at all: `transcript_path`, `cwd`, `tool_status`, `tool_output`, `tool_error` and `duration_ms`. A hook written against the reference would find neither its event name nor its tool arguments. Recorded under open divergences |
| 19 | System prompt and project context | 80 | `vibe/core/system_prompt.py`, `vibe/core/prompts/`, `vibe/core/config/harness_files/` | `AGENTS.md` walk-up, prompt resolution, skill and subagent summaries. Restated 2026-08-19, up from 75: `project_context` is implemented in `crates/vibe-core/src/prompt.rs` and the row's claim that it is missing was stale. Two gaps remain, both declared-without-a-consumer rather than absent. `include_commit_signature`, `include_model_info`, `include_project_context` and `include_prompt_detail` are declared in the registry and read nowhere, so toggling any of the four changes nothing, where `vibe/core/system_prompt.py` gates four sections on them. `system_prompt_id` resolves through the agent settings and the builtin profiles set it, and no consumer composes a session's prompt from it, which `crates/vibe-app-server/src/experiments.rs` records at the point where the reference would refresh the prompt |
| 20 | Agents, subagents, delegation | 72 | `vibe/core/agents/`, `vibe/core/subagents.py`, `vibe/agents.py` | `AgentProfile`, `AgentRegistry`, `SubagentManager`, `agents/{list,install,uninstall}`, and `task` now published conformantly. Restated 2026-08-19, down from 75: the `task` result shape diverges, recorded on the built-in tools row and under open divergences. At v2.24.1 the reference also renamed its default profile from `default` to `ask` and moved its default stance to accept-edits (`vibe/core/agents/models.py`), which this port has not followed; that belongs to the version drift below rather than to this score |
| 21 | Connectors | 70 | `vibe/core/tools/connectors/` | Registry, auth, refresh, toggle. Catalog scope unverified |
| 22 | Teleport and Vibe Code Web | 85 | `vibe/core/teleport/`, `vibe/app_server/_vibe_code.py` | `vibeCode/teleport/*`, `vibeCode/projects/*` and the session-less `projectLinks/*` all present, the last two sharing one saved-link store as upstream does. Teleport workflow states and the push flow are unmeasured against the reference |
| 23 | Autocompletion | 100 | `vibe/core/autocompletion/`, `vibe/cli/autocompletion/` | **Measured by a differential oracle**, restated 2026-08-11 from its printed counts: **158 of 159 comparisons across 5 families** replay the reference's own answers, at 7/8 constants, 65/65 ignore-rule verdicts, 12/12 walks, 34/34 incremental change sets and 40/40 rankings, and the single divergence is the one ledgered non-goal below. The ASCII-mask prefilter is asserted to remove no candidate over 40/40 queries. Reproduce with `cargo test -p vibe-cli --all-features autocompletion_parity_tests -- --nocapture`. `crates/vibe-cli/src/tui/completion/path.rs` holds the index, its compiled ignore rules, its rebuild and incremental-update counters and the ranking, and `crates/vibe-cli/src/tui/completion/path/watch.rs` the workspace watcher `file_watcher_for_autocomplete` gates on every query: a create, modify or delete is applied incrementally, a batch carrying more than 200 changes falls back to a full rebuild, a deleted directory drops every entry under its path prefix and a created one adds every non-ignored descendant. One index is built per workspace root and reused across every completion query rather than rebuilt per query. Residual: the three divergences recorded below, none of which changes an answer a caller reads |
| 24 | Voice (STT, TTS, narrator) | 100 | `vibe/cli/voice_manager/`, `vibe/cli/narrator_manager/`, `vibe/cli/transcribe/`, `vibe/cli/tts/`, `vibe/cli/audio_player/`, `vibe/cli/audio_recorder/` | **Measured by a differential oracle**, restated 2026-08-11 from its printed counts: **1 013 of 1 065 comparisons across 4 families** replay the reference's own answers, at 28/31 constants, 280/292 transcription resolutions, 230/242 speech resolutions and 475/500 wire frames, with every divergence one of the 7 ledgered entries the replay carries and each of those a decided one. Reproduce with `cargo test -p vibe-cli --all-features voice_parity_tests -- --nocapture`. The transcription session's endpoint, model, sample rate, encoding and streaming delay all resolve from `active_transcribe_model`, its `[[transcribe_models]]` entry and that entry's provider, the credential is read under the `api_key_env_var` the provider declares from the environment then the credential store, and a configuration that resolves to no session reports why instead of addressing a default. The narrator posts a completed turn's summary to the resolved speech endpoint with the configured voice and container, decodes the answer and plays it through the default output device, stopping playback on cancellation. Both audio model aliases are offered as choice lists in the voice settings, and the four transcription events and the three read-aloud ones are sent through the session's telemetry client under the same gate as every other event. Residual: playback itself has no oracle, since neither a host without an output backend nor one without a device can be stood up in CI; the decoded-buffer boundary and the absent-backend branch are what tests assert, recorded below rather than counted here |
| 25 | Vibe Code Project | 85 | `vibe/core/vibe_code_project/`, `vibe/app_server/_project_links.py` | Workflow, picker and the `projectLinks` layer present, with the reference candidate ranking and the four root reject reasons. The project API client is measured by fixture rather than against the live service |
| 26 | Compaction | 100 | `vibe/core/compaction/`, `vibe/core/middleware.py` | **Measured by a differential oracle**: 172 scenarios across 8 families replay the reference's own answers with 0 divergent outside a four-entry ledger, at 10/10 token counts, 11/11 truncations, 10/10 summary extractions, 8/8 round drops, 8/8 rendered envelopes, 11/11 parsed envelopes, 16/16 message selections and 98/98 manager scenarios covering the call sequence, the retry ladder, the fallback decision and the two failure reasons. Reproduce with `cargo test -p vibe-core --all-features compaction_parity_tests -- --nocapture`. `crates/vibe-core/src/middleware.rs` holds the policy pipeline the reference resolves ordering with, and `crates/vibe-core/src/compaction/` the pure calculation and the provider-neutral manager. Automatic compaction fires on `auto_compact_threshold` before the request rather than after a provider refusal, a reactive recovery happens at most once per turn and never under `raise_on_compaction_failure`, the envelope preserves the last 20 000 tokens of the operator's own turns, `compaction_model` addresses the summarization, `compaction_prompt_id` resolves the request through the project and user override chain, `context_warnings` injects one warning per session at half the window, the event pair projects as one checkpoint entry carrying all five `CompactionDetails` fields, and a compacted session keeps its stable identity suffix. Residual: the four ledger entries below, three of them licensing and one a telemetry call type no request census carries yet |
| 27 | Telemetry and observability | 100 | `vibe/core/telemetry/`, `vibe/core/tracing.py`, `vibe/observability/`, `vibe/core/log_reader.py` | **Measured by a differential oracle**, restated 2026-08-12 from its printed counts: **1 067 of 1 089 comparisons across 15 families** replay the reference's own answers, at 70/72 constants, 306/306 envelopes, 60/72 metadata censuses, 8/8 attachment counts, 25/26 event names, 71/74 event payloads, 70/70 exporter configurations, 119/119 spans, 17/17 provider names, 36/36 redaction verdicts, 64/64 formatted log lines, 23/23 encodings, 84/84 parse verdicts, 44/48 log pages and 70/70 logging configurations, with every divergence one of the 10 ledgered entries the replay prints and each of those a decided one. Reproduce with `cargo test -p vibe-core --all-features telemetry_parity_tests -- --nocapture`. `crates/vibe-core/src/telemetry.rs` publishes the reference open-properties envelope, the 15-field metadata census and the endpoint, credential and user-agent resolution, gated by `enable_telemetry` read from the merged configuration on every send, with no `--telemetry` flag left to publish. The credential is read under the `api_key_env_var` the active Mistral provider declares, from the environment first and the OS credential store second, as `resolve_api_key` reads one upstream. 25 of the 26 event names are raised with the reference property keys, from the same places the reference raises them. `crates/vibe-core/src/tracing.rs` installs the OTLP HTTP exporter resolved from `otel_endpoint` or from the Mistral provider, emits the four span families with the reference `gen_ai.*` attribute keys, propagates the conversation id through baggage and applies the redaction mode `otel_redaction` selects before export, so none of the three keys is declared-only any more. `crates/vibe-core/src/observability.rs` writes the rotating `$VIBE_HOME/logs/vibe.log` in the reference line format and `diagnostics/logs/read` answers from that file with the real process identifiers, so a line another process wrote is readable. `telemetry/record` ships the client's event under the same envelope through the client the adapter installed, and the ACP `telemetry/send` extension notification routes the two reference event names into it. Residual: the five divergences recorded below, one of which is `vibe.admin_config_applied`, the 26th event name, left undeclared because the org-managed configuration layer it reports on is not composed here, and the OTLP wire itself, which no collector in CI receives |
| 28 | Skills | 100 | `vibe/core/skills/`, `vibe/core/skills/builtins/`, `vibe/core/skills/registry/` | **Measured by a differential oracle**: 209 scenarios across 8 families replay the reference's own answers with 0 divergent outside a seven-entry ledger, at 30/30 frontmatter parses, 38/38 metadata verdicts, 25/26 discovery catalogs, 26/26 filterings, 33/34 command resolutions, 8/8 wire projections, 20/21 store operations and 13/13 manifest operations, plus the builtin catalog compared structurally. Reproduce with `cargo test -p vibe-core --all-features skills_parity_tests -- --nocapture`. Every ledgered case is a decided divergence recorded below: the four builtin prose digests and the `skill-creator` invocation they imply, the deprecated legacy root this port keeps readable, and the store's fallback description. The registry catalog client has no corpus family, because its behaviors are request parameters and error reasons rather than filesystem answers; it is measured by differential unit tests over a recording transport (`crates/vibe-core/src/skills/registry/client_tests.rs`). The registry subtree ships dormant, exactly as upstream, recorded below |
| 29 | Checkpoints | 100 | `vibe/core/checkpoints/` | **Measured by a differential oracle**: 32/32 line fixtures, 36/36 opcode fixtures and 73 engine scenarios replayed against the reference with 0 divergent across all seven families (steps, regions, scopes, projections, anchors, restore plans, log shape) and an empty ledger. `crates/vibe-core/src/checkpoints/` holds the append-only log, the pure read model, the recorder and the filesystem port, with region identity, dependency edges, decision closure, reconstruction and hunk anchors all reproduced. Reproduce with `cargo test -p vibe-core --all-features checkpoint_parity_tests -- --nocapture`. The retention ceiling below is the one recorded divergence |
| 30 | Configuration | 92 | `vibe/core/config/`, `vibe/core/config/layers/`, `vibe/core/config/vibe_schema.py`, `vibe/config_values.py` | **Measured by differential oracle**: 67/68 reference fields declared, published and merged by the strategy the reference declares, 30/30 merge scenarios, 8/8 model-validation scenarios and 22/22 MCP entry scenarios replayed from the committed corpus, plus 8/8 reference `config/*` methods dispatched. Seven layers compose (Defaults, Discovered, SelectedToml, Experiments, Environment, Runtime, Agent). Residual: the GrowthBook layer and the per-layer async state machine are recorded divergences below, the fingerprint token has its own format, 5 keys this port declares have no upstream counterpart, and the `b78b451` re-pin left 1 v2.24.0 field undeclared here, recorded below. Declaring a key is not implementing its feature: each arrives with the feature that reads it, the five compaction keys arrived with the compaction parity work, and `browser_auth_base_url` and `browser_auth_api_base_url` left the declared-only set with the setup parity work, each proven consumed by a test that changes the key and observes the change: `a_changed_api_base_key_changes_the_request_host` asserts the issued sign-in request follows the API key, and `explicit_urls_win_over_the_defaults` with `an_explicitly_empty_url_disables_sign_in_rather_than_defaulting` assert the base key both addresses the console and gates the flow, all three in `crates/vibe-core/src/auth/sign_in_http_tests.rs`. Amended 2026-08-19: the one undeclared v2.24.0 field is named here rather than left as a count. `show_greeting` is declared nowhere in this port, and `vibe/app_server/_projection.py` carries it into `ConfigView`, so `config/read` and `runtime/read` answer without it. 18 of the 68 reference fields are declared, defaulted, published and merged with no reader outside the registry, which is the standing policy this row states: a key is declared, defaulted, published and merged when the reference declares it, and read when the feature that reads it ships |
| 31 | Setup, onboarding, authentication | 100 | `vibe/setup/`, `vibe/core/auth/` | **Measured by two differential oracles**: 182 comparisons across 6 authentication families and 491 across 4 onboarding families replay the reference's own answers with 0 divergent outside a ledger that is empty in both, at 41/41 auth states over the five-source matrix, 21/21 persistence and keyring-migration effects, 41/41 sign-in service and gateway behaviors, 33/33 URL verdicts, 23/23 error codes and their prose inequalities, 6/6 ACP label digests, 313/313 screen-graph fields, 25/25 terminating values, 86/86 domain validations and 60/60 original screen sentences, plus both constants blocks. Reproduce with `cargo test -p vibe-core --all-features setup_auth_parity_tests -- --nocapture` and `cargo test -p vibe-cli --all-features onboarding_parity_tests -- --nocapture`. `crates/vibe-core/src/auth/` holds the provenance classification with the reference's five-level precedence, the credential store under the reference service name with migration on read from both the reference legacy name and this port's prior-build name, the persistence that falls back to `$VIBE_HOME/.env` when the store refuses a write, and the PKCE `S256` sign-in service with its polling state machine, its eleven error codes and the origin and path-prefix validation every server-supplied URL passes before any request or browser opens. `crates/vibe-cli/src/tui/onboarding/` installs the reference's seven screens under its own gating predicate, the three steps with no reference counterpart are retired, and workspace trust is decided by the pre-session dialog that persists it. `crates/vibe-acp/src/auth.rs` advertises the three authentication methods under the reference's client-capability gates and serves `auth/status` and `auth/signOut`, which is the product's only credential removal path. Residual: the prose divergences below, the deliberate poll cadence, and `account/read`, which still cannot answer `unauthorized` |
| 32 | Experiments and rollouts | 100 | `vibe/core/experiments/`, `vibe/core/config/layers/growthbook.py` | Restated 2026-08-19, up from 25: the engine was ported on 2026-08-17, five days after the row was last read. `crates/vibe-core/src/experiments/` holds the client, the manager, the models, the session state and the recorder against `vibe/core/experiments/`, `crates/vibe-core/src/config/experiments_layer.rs` composes the layer, and `crates/vibe-core/src/experiments_parity_tests.rs` measures it. **Measured by a differential oracle**, restated to 100 on 2026-08-19 from its printed counts: **502 of 502 comparisons across 13 families** replay the reference's own answers with an empty ledger, at 63/63 eval failures, 168/168 session gates, 44/44 configuration mappings, 33/33 variant resolutions, 30/30 attributes, 20/20 configuration variants, 18/18 variant labels, 14/14 feature resolutions and 12/12 layer precedences. Reproduce with `cargo test -p vibe-core --all-features experiments_parity_tests -- --nocapture`. `crates/vibe-core/src/experiments/client.rs` issues the eval request the reference declares, with `attributes` and `forcedVariations`, and issues none at all when no client key resolves |
| 33 | VS Code extension promo | 10 | `vibe/cli/vscode_extension_promo/` | Not ported |
| 34 | Conversation engine (agent loop) | 70 | `vibe/core/agent_loop/`, `vibe/core/types.py`, `vibe/core/middleware.py`, `vibe/core/plan_session.py` | Added 2026-08-19. `vibe/core/agent_loop/_loop.py` is 2 912 lines and `crates/vibe-core/src/engine.rs` 3 580, and no differential oracle compares them: this is the largest unmeasured part of the port and the reason the weighted total below carries more uncertainty than any single row. Reproduced by named tests rather than by replay: the turn loop, concurrent tool execution, the retry ladder, the cancellation path, tool-call answering so no call is left unanswered, plan review, context injection, skill invocation and child sessions. The score is a structural judgement over the two files, not a measurement, and it stays a judgement until an oracle exists |

## Version drift: v2.24.0 to v2.24.2

Measured 2026-08-19 against reference `5e6aa0f`. This section is not a task
list; it is the size of the gap between the pinned target and the live one, so
the second score above can be read rather than trusted.

The app-server surface is the worst of it. Upstream now declares 96 methods
where the pin declared 91. **82 of the 96 are declared here, 14 are not, and 9
methods this port declares no longer exist upstream.** Seven of those 9 were
renamed rather than dropped, and the new name is one of the 14 missing:
`config/patch` became `config/write`, `callback/respond` became
`callback/result`, `history/list` became `session/history/list`,
`session/close` became `session/stop`, `session/compact/start` became
`session/compact`, `session/title/update` became `session/rename`, and
`shell/run` became `session/shellCommand`. The remaining two, `shell/interrupt`
and `config/thinking/write`, have no successor of their own. The seven other
missing methods are new: `events/read`, `plugin/info`, `plugin/reload`,
`session/history/get`, `session/turns/list`,
`workspace/trust/untrustedConfig` and `workspace/worktrees/remove`. An editor
written against today's `vibe/app_server/protocol.py` would fail on most of a
session's lifecycle here, and the failure would be a method-not-found on a name
this port never learned rather than a behavioral difference.

Behind that rename sits a rebuilt backend: `vibe/app_server/_session_backend_port.py`,
`vibe/app_server/_session_backend_services.py`, `vibe/app_server/_legacy_session_backend.py`
and `vibe/app_server/_legacy_session_runtime.py` are all new, and
`vibe/core/session/session_interop.py` with them.

Four other changes are user-visible on their own. `vibe/core/agents/models.py`
renamed the default agent profile from `default` to `ask` and moved its default
stance to accept-edits, so `--agent ask` and a `default_agent = "ask"`
configuration both fail here. `vibe/core/worktree.py` was replaced by
`vibe/core/git/worktree/` and `vibe/core/git/repo.py`, which is where the
worktree row's reference now lives. `vibe/core/config/vibe_schema.py` gained
`log_level` and `allowed_models`, neither declared here, with
`vibe/cli/textual_ui/widgets/log_level_picker.py` and a `/log-level` command
behind the first. `vibe/setup/auth/whoami.py` is new and is the console client
the `account/read` divergence below records as absent.

The tool surface is the reassuring half: no tool was added or removed between
the two releases, and the only signature change is `file_path` moving from `str`
to a validated `ToolPath` alias in `vibe/core/tools/utils.py`, which publishes
the same JSON Schema. The notification vocabulary is unchanged.

One change is context rather than parity, and it is the most consequential thing
in the diff. v2.24.1 added `--experimental-harness` (`vibe/_experimental_harness.py`,
`vibe/app_server/_unified_harness_backend_adapter.py`), which swaps the Python
app-server's session backend for a `mistralai_rust_harness` distribution.
Upstream is moving its own core to Rust. Nothing in this document depends on
that; the strategy of this repository might.

## Open divergences

Accepted divergences below are decided. These are not: each is a place this port
answers something the reference does not, with no decision recorded and no
ledger entry holding it. They are listed newest first, all found by the
2026-08-19 re-read, and each names the reference file that publishes the
contract and the file here that breaks it.

| Part | What diverges | Reference | Here |
|---|---|---|---|
| No production path runs a hook | `HookManager` is constructed three times in this repository, all three inside the `#[cfg(test)]` block of its own crate, and no other module calls its chain. `hooks.toml` is discovered, validated and counted, and never executed. This is the divergence that has to close before the payload one below can matter to anyone | `vibe/core/hooks/manager.py`, `vibe/core/agent_loop_hooks.py` | `HookManager` in `crates/vibe-core/src/extensions/hooks.rs`, called from nowhere |
| The hook invocation written to stdin | The reference writes `invocation.model_dump_json()`, so a hook reads thirteen snake_case fields. This port writes seven in camelCase: four are the same name in another case, `hook_event_name` becomes `kind` with the reference's own values, `tool_input` becomes `payload`, `tool_output_text` becomes `outputText`, and `transcript_path`, `cwd`, `tool_status`, `tool_output`, `tool_error` and `duration_ms` are absent. Reading the hook's answer is conformant, so the divergence is one-directional | `vibe/core/hooks/models.py`, `vibe/core/hooks/executor.py` | `HookInvocation` in `crates/vibe-core/src/extensions/hooks.rs` |
| The `task` result shape | The reference publishes `response`, `turns_used` and `completed`. This port publishes a delegation effect: `parentSessionId`, `childSessionId`, `publicSessionId`, `status` and `result`. The agent loop renders a typed result field per line into the model's transcript, so the divergence reaches the conversation | `vibe/core/subagents.py` | `DelegationEffect` in `crates/vibe-core/src/extensions/subagents.rs`, published by `crates/vibe-app-server/src/client/live/delegation.rs` |
| The `web_fetch` result keys | The reference publishes `content_type` and `was_truncated`; this port publishes `contentType` and `wasTruncated`. `skill` and `web_search` publish the reference spelling, so the divergence is local to one tool | `vibe/core/tools/builtins/web_fetch.py` | `crates/vibe-core/src/tools/builtins/web_fetch.rs` |
| `vertex-anthropic` has no transport | The style is accepted and routes to the Anthropic body and headers, and none of the Vertex transport exists here: no Application Default Credentials, no token refresh, no regional `{region}-aiplatform.googleapis.com` base, and neither the `rawPredict` nor the `streamRawPredict` endpoint. A configuration naming the style resolves and cannot reach the service | `vibe/core/llm/backend/vertex.py` | `ProviderStyle::VertexAnthropic` in `crates/vibe-core/src/provider.rs` |
| ACP serves 8 of 10 commands | `leanstall` and `unleanstall` are advertised to editor clients upstream and to terminal clients here, and this port's ACP adapter does not serve them | `vibe/acp/commands/registry.py` | `Command` in `crates/vibe-acp/src/commands.rs` |
| `show_greeting` is undeclared | The one v2.24.0 configuration field with no declaration here. It also travels in the projected configuration view, so two answered methods are short a field rather than only the file being short a key | `vibe/core/config/vibe_schema.py`, `vibe/app_server/_projection.py` | absent from `crates/vibe-core/src/config/registry.rs` |
| Four `include_*` keys are declared and unread | `include_commit_signature`, `include_model_info`, `include_project_context` and `include_prompt_detail` gate four sections of the reference system prompt. Each is declared in the registry here and read nowhere, so setting any of them changes no prompt. This is the standing declared-without-a-consumer policy applied to keys whose feature has otherwise shipped, which is what makes it a divergence rather than a backlog item | `vibe/core/system_prompt.py` | `crates/vibe-core/src/config/registry.rs` |

## Accepted divergences

Some parts cannot reach 100 through code alone. For these, 100 means a decided
and documented divergence, not a port. Recording the decision early keeps it out
of every subsequent parity review. Each row names why the divergence stands and
what in the repository holds it in place.

| Part | Reason | Evidence |
|---|---|---|
| Tool description text | `NOTICE` forbids shipping upstream prose, so the 13.8 KB of reference directive text has no byte-identical counterpart here and never will. Descriptions are held to directive coverage and compared for presence only. **This does not cap the score in this document**, measured 2026-08-06 and settled two ways. By stated method: the scoring unit above is names and schemas, and description text is not among the inventories it diffs. By construction: the instrument that produces the tool scores replaces every description with `<described>` in both the capture and the replay, so prose carries exactly zero weight and no amount of it can move the number. The residue is therefore a licensing fact with no scoring consequence here | `DESCRIBED` in `scripts/parity/tool_surface.py` and `crates/vibe-app-server/src/tool_surface_parity_tests.rs`, which substitute every description before diffing, plus the `## Method` section above |
| A third-party score weighing description text | Not determinable from this repository, and recorded as such rather than left open. The externally reported 35/100 score weighs names and schemas "in an unpublished way", so no method exists here to confirm or refute against. The exposure is bounded instead of guessed: measured at the pinned commit, the reference publishes 13 776 bytes of tool prompt prose and 1 735 bytes of parameter descriptions against 3 789 bytes of description-free schema and 94 bytes of names. A byte-weighing external metric would therefore hold roughly four fifths of the tool contract permanently out of reach, which would make that metric a measure of licensing posture rather than of engineering parity. Chasing it is refused on that basis | `vibe/core/tools/builtins/prompts/*.md` at `b78b451`, byte-counted out of tree; no prose is stored here |
| Three shell commands ask where the reference grants | The reference resolves `cat $(which ls)`, `cat file.txt > out.txt` and `git diff --no-index /etc/passwd /dev/null` to `always`, because each one is composed only of allowlisted words. What runs is not what the words say: a substitution runs a program the text never names, a redirect writes to a target the segment never carries, and `--no-index` reads a path the index never held. This port withholds the automatic grant in those three places and asks instead. Nothing is refused that the reference allows, so the divergence costs an approval prompt and never a capability | `STRICTER_THAN_THE_REFERENCE` in `crates/vibe-core/src/shell/shell_parity_tests.rs`, which fails both when the set grows and when a listed case stops diverging |
| `grep` returns its matches sorted | `rg` walks in parallel and prints whichever file finished first, so its match order is not a contract anything can conform to: one capture of the pinned reference recorded two different orders for the same query in a single run. This port sorts by path and then by line, and the capture script sorts the reference's output the same way before recording it, so the match *set* is still compared byte for byte while the order is normalized out on both sides | `stabilize` in `scripts/parity/tool_execution.py` and the sort in `crates/vibe-core/src/workspace/search.rs`, replayed by `tool_execution_parity_tests` |
| Warning and applied-edit message wording | `read_file` answers an empty file or an out-of-range offset with a sentence, and `edit` reports what it changed with one. Both are authored prose, which `NOTICE` forbids shipping and the PRD lists as a non-goal: a message is held to naming the same cause, value and limit, not to the reference's words. The divergence is bounded to those two fields and every other field of the same result still matches | `LICENSING` entries in `crates/vibe-app-server/src/tool_execution_parity_tests.rs`, which fail if the gap widens past those pointers or narrows to none |
| The checkpoint log holds at most 512 MiB | The reference constructs its log unbounded (`vibe/core/checkpoints/checkpointer.py:56`) and holds it for one agent loop's lifetime. This port holds one log per attached session inside a server process that outlives any single conversation, so an unbounded log is an unbounded process. The ceiling refuses further capture rather than trimming: a region identity a client already holds must never stop resolving because the log made room behind it. Reaching it publishes one warning on `diagnostics/list` and leaves the file writes themselves untouched | `RETAINED_BYTES_LIMIT` in `crates/vibe-core/src/checkpoints/checkpointer.rs`, asserted by `the_log_is_never_shortened_behind_the_caller` and `a_full_log_refuses_the_capture_and_publishes_it` |
| A rewindable entry identity is derived, not stored | The reference reads a message's own `message_id` and falls back to `history:{index}:{role}` when it has none (`vibe/app_server/_projection.py:607`). A message this port persists carries no identifier, so every rewindable entry resolves through that fallback. The identity is therefore stable for as long as the stored list is, which is exactly what a rewind cuts, and it moves when a compaction rewrites the list, at which point the checkpoint log is cleared with it | `history_entry_id` in `crates/vibe-app-server/src/workspace.rs`, asserted by `rewind_resolves_an_entry_identity_and_forks_before_the_selected_message` |
| `edit` decodes a narrower codec set | The reference tries the byte-order mark, UTF-8, the locale codec and then `charset-normalizer`, a statistical detector. This port recognizes the mark, UTF-8, and falls back to Latin-1, which decodes every byte, so no file is refused and every one of the three round-trips byte for byte. A file in a multi-byte codec with no mark decodes as Latin-1 and is written back unchanged unless the edit touches it | `crates/vibe-core/src/workspace/text_file.rs` and its round-trip tests |
| `grep` is confined to the workspace root | The reference resolves the search path against its working directory and lets the permission chain answer for a path outside it. This port refuses it at the boundary instead, which is the confinement every other file tool here already applies. Nothing outside the workspace becomes readable that was not readable before | `Workspace::confined`, reached by `workspace::search` |
| A telemetry endpoint is HTTPS only | The reference derives the datalake endpoint from the active Mistral provider's `api_base` with a regex that admits `http://`, and posts a bearer token to whatever it resolves. This port refuses a resolved endpoint that is not HTTPS, or that carries a credential of its own, and answers no target instead, which silences telemetry rather than sending the key in the clear. Nothing the reference reaches over HTTPS becomes unreachable; the corpus's eight documents all resolve, and the `envelope` family conforms on every one | `telemetry_endpoint` in `crates/vibe-core/src/telemetry.rs`, asserted by `the_endpoint_is_derived_from_the_provider_base` |
| The crash reporter is absent | The reference ships Sentry dormant at the pinned commit: `_CLI_SENTRY_DSN` and `_ACP_SENTRY_DSN` are both `None` (`vibe/observability/sentry.py:15-16`), so `sentry_sdk.init(dsn=None)` never initializes and `init_sentry` always answers false through its own `is_initialized` check (177-209). Adding `sentry-rust` would create a capability the reference does not have, which is the same reasoning as the dormant remote registry row below; this port initializes no crash reporter at all, and what a crash leaves behind is the log file at `$VIBE_HOME/logs/vibe.log`, which the same PRD shipped. Revisit only if the reference ever sets a DSN, which is measured rather than assumed: the corpus records both DSNs as configured-or-not and the replay compares each against false | `constants/sentry` in `crates/vibe-core/src/telemetry/telemetry_parity_tests.rs`, captured by `scripts/parity/telemetry.py` and asserted by `the_committed_corpus_replays_against_this_port`, which fails the moment either DSN stops being null, as does the recapture probe |
| `user_plan` is carried and never filled | The reference fills the census field from the console's `whoami` answer, mapping its plan type and plan name onto four vocabularies (`vibe/app_server/_account.py:98-115`, set on the loop at 220). This port classifies `ready`, `missing_key` and `disabled` locally and has no client for that endpoint, which is the `account/read` row below; none of the three carries a plan name, so the field is absent rather than fabricated from a status that does not name a plan. The census declares it and it travels the moment a console client exists | `TelemetryBaseMetadata::user_plan` in `crates/vibe-core/src/telemetry.rs`, produced from `TelemetryContext::user_plan`, and the `account/read` row below, which records the missing console client |
| `vibe.admin_config_applied` has no producer | The reference raises it where an org-managed configuration layer is applied (`vibe/app_server/_resources.py:617-642`), reporting the outcome, the number of enforced keys and whether an error occurred. This port composes no such layer: its seven configuration layers are Defaults, Discovered, SelectedToml, Experiments, Environment, Runtime and Agent, so there is no application for the event to report on. The name is left undeclared rather than shipped without a producer, which is what keeps the vocabulary honest, and it arrives with the layer if the layer ever does | `TelemetryEvent` in `crates/vibe-core/src/telemetry.rs` publishes 25 of the 26 names, and the four `admin_config_applied` entries of the ledger in `crates/vibe-core/src/telemetry/telemetry_parity_tests.rs` fail the replay the moment the gap closes |
| The compaction envelope's two prose runs | `NOTICE` forbids shipping upstream prose, and the envelope carries two runs of it: the sentences that open it and the one that introduces the summary. This port writes its own covering the same three directives, so the envelope reads the same and no reference sentence ships. Everything its readers depend on is held to the reference byte for byte: the four reserved tags, the line layout, one element per preserved message, the escaping and the summary block. The corpus records each prose run's length and SHA-256 so a change on either side is still detected | `envelopeProse` in `crates/vibe-core/src/compaction/compaction_parity_tests.rs`, whose two ledger entries fail the replay if either run ever conforms, against `envelopeRenders` 8/8 conforming on structure |
| Builtin skill prose | The reference seeds two builtin skills, `vibe` and `skill-creator`, whose descriptions and bodies total 40 589 bytes of authored prose `NOTICE` forbids shipping. This port seeds the same two names with the same structure, invocability split, `builtin` source and pathless projection, and writes both descriptions and both bodies originally against the same directive coverage: the `vibe` body documents the home, configuration, models, agents, skills, tools, commands, flags, hooks, MCP, connectors, trust, mentions, logs, themes and voice of this port, with its documentation link pinned to the running `[workspace.package]` version, and the `skill-creator` body covers one-question-at-a-time gathering, the `SKILL.md` shape, the five discovery roots, the reserved names, support files and the permission flow. The corpus records each reference prose run as length plus SHA-256, so structure is compared for equality and prose for permanent inequality | The four `builtins/builtinProse-*` entries in `DIVERGENCES` in `crates/vibe-core/src/skills/skills_parity_tests.rs`, plus `command/builtin-skill-creator` for the slash invocation that delivers the `skill-creator` body, whose stale check fails the replay the moment either body or description conforms to the reference digest, so the divergence can never be closed by copying |
| The registry store's fallback description | A materialized registry skill whose payload carries no description is published under a generated sentence, and the reference's sentence is authored prose `NOTICE` forbids shipping. This port writes its own naming the same two facts, the skill's name and its registry provenance, and the corpus records the reference's only as length plus SHA-256, masked as `{fallbackDescription}` in every recorded tree, so the store comparison stays structural while the prose stays original | `fallback_description` in `crates/vibe-core/src/skills/registry/store.rs` and the `store/fallbackDescription` entry in `DIVERGENCES` in `crates/vibe-core/src/skills/skills_parity_tests.rs`, whose stale check fails the replay the moment the sentence conforms to the reference digest |
| The remote registry ships dormant | The reference implements the registry catalog client, version store and manifests and then never imports any of it: at the pinned commit the subtree is reachable only from its own tests, `experimental_enable_registry_skills` has exactly one occurrence in the whole tree (its own declaration), and no wire method, CLI command or response field publishes it. This port reproduces that state rather than inventing the missing surface: the four modules are ported with their safety rules and measured by the `store` and `manifest` corpus families plus the client's differential unit tests, the experiment key is read from the merged configuration as the subtree's one gate, and enabled or not the catalog is unchanged, because there is no published load lifecycle to reproduce (skills-parity PRD, open question 3). Inventing a `skills/install` method would fail the app-server surface oracle on an invented name | `published_skills` in `crates/vibe-core/src/skills/registry.rs`, its gate in `WorkspaceService::catalog`, asserted by `the_registry_experiment_key_gates_a_dormant_subtree` in `crates/vibe-app-server/src/workspace.rs`, and the 0-invented-names count `app_server_surface_parity_tests` prints |
| The placeholder a failed summarization falls back to | Outside strict mode the reference still compacts when neither summarization call produced a usable summary, and the summary it writes into the envelope is an authored sentence `NOTICE` forbids shipping. This port writes its own, so the conversation degrades the same way and no reference sentence ships; the decision tree around it, both failure reasons, the fallback and the retry ladder, is held to the reference exactly | `placeholderSummary` in `crates/vibe-core/src/compaction/compaction_parity_tests.rs`, whose ledger entry fails the replay if the wording ever conforms |
| The compaction request carries no telemetry call type | The reference labels its summarization request `secondary_call`, the call type its telemetry client reads back into the request census. Every model call this port makes now reports `vibe.request_sent`, and each one reports `main_call`: the summarization request is made through the same path as the turn's own, so it is marked through the provider metadata instead of a census of its own. The request itself, its transcript copy, its tools and its tool choice are the reference's | `managerCallType` in `crates/vibe-core/src/compaction/compaction_parity_tests.rs`, whose ledger entry fails the replay when a call type starts conforming, against `TelemetryCallType` in `crates/vibe-core/src/telemetry.rs` |
| The context warning's message is original prose | `NOTICE` forbids shipping the reference's warning sentence, so this port writes its own naming the same three quantities: the share of the window consumed, the current token count and the window. Only the `vibe_warning` tag is reproduced verbatim, because it is an identifier a client matches on rather than prose | `ContextWarningMiddleware` in `crates/vibe-core/src/middleware.rs`, asserted by `the_context_warning_fires_once_at_half_the_window` |
| The context-warning latch clears on the compaction reset alone | The reference clears the latch on either reset reason, and resets its pipeline only when the conversation is compacted or cleared. This port also resets at the top of every turn, so clearing on both reasons would warn once per turn instead of once per session. Clearing on the compaction reason alone is what reproduces the reference's observable cadence under this port's own | `ContextWarningMiddleware::reset` in `crates/vibe-core/src/middleware.rs`, asserted by `the_context_warning_is_silent_without_a_window_and_relatches_on_a_compaction` and by `the_context_warning_reaches_the_model_once_per_session` in `crates/vibe-app-server/src/client.rs` |
| `identity/read` and `workspace/worktrees/list` are declared and unrouted | Both names arrived upstream at v2.24.0 and the `b78b451` re-pin brought them into the inventory, so the earlier statement that they exist nowhere in the reference tree is false at this pin and was removed. They are declared in `SERVER_METHODS`, so a client reading the inventory sees the same names, and routing them is app-server parity work rather than compaction work | `UNROUTED_METHODS` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs`, which fails the replay when either becomes routed |
| `ConfigView` omits the three unpinned-model fields | v2.24.0 added `activeModelPinned`, `defaultModelAlias` and `showGreeting`, all three produced from an `active_model` that may be left unpinned and resolved on read. Producing them means porting that feature, which is configuration parity work; until then `config/read` and `runtime/read` are recorded as diverging from the census rather than validated against a shape this port cannot fill | `DIVERGENT_RESPONSES` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs`, which fails the replay when either response starts validating |
| `PublicRetryCategory` is unmodeled | The vocabulary was recorded as absent until v2.24.0 reached it from `turn/error`. Nothing in this port classifies a retry, so the name is recorded rather than answered with a vocabulary no code produces | `UNMODELED_ENUMS` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs` |
| 1 configuration field the v2.24.0 reference declares is undeclared here | `show_greeting` is the last of the four v2.24.0 keys left: no experiment targets it and its consumer is the `ConfigView` row above, which is app-server parity work. The other three arrived with the resolution that gives them an effect and left this list. Declaring a key without that resolution would publish a schema whose values change nothing, which is the failure this document names | `UNDECLARED_FIELDS` in `crates/vibe-core/src/config/surface_parity_tests.rs`, which fails the replay when it becomes declared |
| The eleven sign-in error sentences | The reference names each browser sign-in failure in an authored sentence `NOTICE` forbids shipping. This port produces the same eleven codes in the same declaration order from the same conditions, and writes its own sentence for each, naming the same cause and the same next action. The corpus records every reference sentence as a length plus a SHA-256, so codes and order are compared for equality while the prose is compared for permanent inequality: a sentence here that ever matches a reference digest fails the replay, so the divergence can never be closed by copying | The `errorTaxonomy` prose comparison in `crates/vibe-core/src/setup_auth_parity_tests.rs`, running 23/23 conforming on the codes, their order and the inequality of all eleven sentences |
| The onboarding screen text | Every heading, subtitle, option label, hint and status line the reference's seven onboarding screens draw is authored prose, and the same is true of the sentences its exit paths print. This port writes its own for each, covering the same directives, and the corpus records the reference's runs per module as a length plus a SHA-256 and never as text. The replay digests every sentence this port's onboarding modules carry and fails if one ever matches. Three runs are reproduced on purpose and listed with their reason, each a name rather than prose: the product name, the organization name, and the greeting fragment that introduces the product | `screenProse` and `REPRODUCED_NAMES` in `crates/vibe-cli/src/tui/onboarding_parity_tests.rs`, at 60/60 conforming, plus the `terminating` message digests in the same module; each reproduced name is stale-checked and fails once it stops matching |
| The ACP authentication method labels | The reference names and describes each editor-protocol authentication method, and labels the terminal one, in authored prose an editor renders verbatim. This port advertises the same method ids under the same client-capability gates and writes its own name, description and label for each. The `acpAuthProse` family records the five reference runs as length plus SHA-256 only, and the guard asserts both directions: every published label differs from the reference's, and every label is still a real sentence rather than an empty string that would differ for the wrong reason | `crates/vibe-acp/src/auth/prose_tests.rs`, reading the `acpAuthProse` family of `crates/vibe-core/tests/setup-auth/corpus.json`, whose surface list must match what the crate publishes so no label goes unchecked |
| The sign-in poll interval is 3.0 seconds with no backoff | RFC 8628 §3.5 recommends a 5 second poll interval with a `slow_down` escalation, and the reference polls at 3.0 seconds with neither. Parity wins over the recommendation deliberately: a different interval issues a different number of requests over the same window, which is a divergence the corpus measures directly, and the loop is already bounded by the server's recorded expiry. The deviation is recorded here rather than silently inherited | `POLL_INTERVAL_SECONDS` in `crates/vibe-core/src/auth/sign_in.rs`, asserted by `constants/pollIntervalSeconds` and by the per-scenario `sleeps` of `signInProtocol` in `crates/vibe-core/src/setup_auth_parity_tests.rs`, either of which fails if the cadence changes or a backoff appears |
| Onboarding is measured as observations, not as rendered cells | Textual and ratatui cannot produce the same cells, so no story asserts terminal output against the reference's snapshots. The capture drives the real reference app through Textual's headless pilot and records the installed screen set, the transitions taken, the focus target per screen, the validation class per input state, the effects persisted and the terminating value; no cell, SVG or styled text enters the corpus. The precedent is the chat-input harness, which established the same measurement for the composer | `scripts/parity/onboarding.py` and `crates/vibe-cli/src/tui/onboarding_parity_tests.rs`, whose corpus carries observations only and whose screen-graph family compares 313 fields per replay |
| The update prompt installs by rerunning the installer | The reference offers an update from inside its dialog, runs the package-manager upgrade commands it was installed through, and reports one of four outcomes. This port now publishes the same four outcomes and the same exit codes, and runs its own upgrade commands: the shell and PowerShell installers this repository ships, because no package manager publishes this binary. The dialog, its choices, its labels and its two modes are the reference's; the divergence is which command runs and the manual sentence the failed branch prints, which is authored prose `NOTICE` forbids reproducing | `the_upgrade_commands_rerun_an_installer_this_repository_publishes` in `crates/vibe-cli/src/distribution/release_parity_tests.rs`, which fails the moment the list stops being one installer rerun resolved from `[package] repository` and becomes a package-manager upgrade the way `UPDATE_COMMANDS` is; the list itself is `upgrade_commands` in `crates/vibe-cli/src/distribution.rs`, `UpdatePromptResult` and `update_failed_message` are in `crates/vibe-cli/src/tui/updates.rs` and `run_upgrade_commands` in `crates/vibe-core/src/updates.rs`; the message is replayed as a declared divergence against the committed trace by `crates/vibe-cli/src/tui/runtime_parity_tests/terminal_services.rs` |
| `account/read` cannot answer `unauthorized` | The reference reaches `GET {console_base_url}/api/vibe/whoami` with the bearer credential and maps the answer onto its plan vocabulary. This port classifies three of the four statuses locally and has no client for that endpoint, so a key that is present but revoked reads as `ready` here and `/whoami` shows a null plan. Closing it needs a console client with its own credential handling, which is app-server parity work rather than setup work, and the setup parity work listed it as a non-goal for that reason | The documented classification in `crates/vibe-app-server/src/workspace.rs`, which reaches `ready`, `missing_key` and `disabled` and never claims the console's verdict |
| The standalone `vibe-app-server` command is absent | The reference declares three console scripts in `[project.scripts]` and ships `vibe-app-server.spec`, but its own build workflow runs `build-pyinstaller-binaries.sh vibe-acp.spec vibe.spec` and never builds the third (`.github/workflows/build-and-upload.yml` at the pin), so the only channel that publishes `vibe-app-server` is the PyPI install `[project.scripts]` feeds, which this port does not use. The app server is not absent here, only its standalone stdio command: `vibe` constructs an `AppServer` in process and `vibe-acp` links the same crate, so both published binaries serve the same session methods. Adding a third binary would publish an entry point the reference's own release path does not carry | `PUBLISHED_BINARIES` and `the_release_publishes_exactly_the_two_binaries_the_ledger_records` in `crates/vibe-cli/src/distribution/release_parity_tests.rs`, which fails on a third packaged binary, on a third publishable `[[bin]]` in the workspace, and on either installer naming `vibe-app-server` |
| The release notes are compiled into the binary | The reference resolves `VIBE_ROOT / "whats_new.md"` at run time and answers nothing when the file is missing (`vibe/cli/update_notifier/whats_new.py`), which is what a PyInstaller onedir layout needs. This port embeds the file with `include_str!`, so the notes ship inside the executable and an archive that drops the file still shows them. The observable contract is the stronger one: the reference can degrade to no notes and this port cannot, and nothing upstream depends on replacing the file beside the binary | `whats_new_content` in `crates/vibe-cli/src/tui/updates.rs`. Its `include_str!` fails the build if the file stops shipping, and its `Option<&'static str>` return is what `release_notes_are_present_and_bounded` binds, so closing the divergence by reading a runtime root cannot compile against that signature and has to move this row with it; the heading is held to `[workspace.package] version` by `the_release_notes_heading_names_the_workspace_version` in `crates/vibe-cli/src/distribution/release_parity_tests.rs` |
| Production resolves the GitHub gateway where the reference resolves PyPI | The reference implements both adapters and constructs only one: `PyPIUpdateGateway(project_name="mistral-vibe")` at `vibe/cli/cli.py:351` and `vibe/cli/textual_ui/app.py:4402`, while `GitHubUpdateGateway` is exported from `vibe/cli/update_notifier/__init__.py` and constructed nowhere outside its own tests. This port publishes GitHub release archives and no PyPI package, so resolving the reference's choice would compare a user's binary against a different product's index and advertise a command that installs that product. Both adapters are ported and both stay measured; the divergence is the resolution, not the surface | `production_update_gateway` in `crates/vibe-cli/src/tui/startup/update.rs`, held to the repository `[package] repository` declares by `the_update_gateway_names_the_repository_the_manifest_declares` in `crates/vibe-cli/src/tui/startup/update/gateway_tests.rs`; the retained `PyPiUpdateGateway` stays measured by the 13-event `update-gateway` trace in `crates/vibe-cli/tests/runtime-parity/terminal-services.json`, which fails if the adapter is removed rather than merely unresolved |
| Experiments and GrowthBook | Requires access to a third-party Mistral service with credentials this repository does not hold. The `experiments` table stays as the injection point | The `experiments` field of `crates/vibe-core/src/config/registry.rs`, declared and published with no consumer |
| VS Code extension promo | Advertises an extension that does not target this binary | No promo surface exists in `crates/`, and the parity table scores the part 10 for that reason |
| Configuration fingerprint format | Upstream builds the token from `st_dev:st_ino:st_mtime_ns:st_size` (`vibe/core/config/fingerprint.py:30`); this port digests the file contents. The token is opaque, is only ever compared against one produced by the same implementation, and a content digest detects an edit that restores the size and timestamp | `crates/vibe-core/src/config.rs`, `fingerprint_optional` and the concurrent-edit tests |
| Per-layer async state machine | Upstream caches each layer, forces reloads and transitions trust per layer (`vibe/core/config/layer.py:263`); this port recomposes from disk on every `load()`, which is observably equivalent for every current caller | `load` in `crates/vibe-core/src/config.rs`, which recomposes the selected layers from disk on every call |
| The completion index answers in relative-path order | The reference holds a rebuilt index in `scandir` order and sorts only after an incremental update invalidated its cached order (`vibe/cli/autocompletion/file_indexer/store.py:63-83`), so what a rebuild returns is whatever the filesystem enumerated and is not a contract anything can conform to. This port holds one map keyed by relative path, which is that order permanently. Both sides are compared under the same normalization the `grep` row above already records: the capture sorts the reference's snapshot by relative path before recording it, and the replay reads a map already in that order, so the entry sets are compared and the enumeration order is not | `WorkspaceIndex` in `crates/vibe-cli/src/tui/completion/path.rs`, whose `entries` is a `BTreeMap` keyed by the relative path, and `_entries` in `scripts/parity/autocompletion.py`, which sorts the reference snapshot before it is recorded |
| The index rebuild has no background executor | The reference rebuilds on a thread-pool executor, cancels the task belonging to a root before starting another and tracks the root in flight (`vibe/cli/autocompletion/file_indexer/indexer.py:116-170`). This port rebuilds synchronously inside the completion reader, which is already off the terminal event path, so the executor, its per-root cancellation and its `_target_root` bookkeeping have no observable consequence for any current caller: the same entries, the same counters and the same ranking answer the same query. Same reasoning as the per-layer async state machine row above | `WorkspaceIndex::rebuild` in `crates/vibe-cli/src/tui/completion/path.rs`, which walks the root and replaces the map in one call |
| `WALK_SKIP_DIR_NAMES` has no counterpart | The reference exports the constant from `vibe/cli/autocompletion/file_indexer/ignore_rules.py:84-89` and imports it nowhere at the pinned commit, so reproducing it would ship a constant with no consumer and no behavior to measure. The absence is recorded by the autocompletion oracle as a ledgered divergence rather than as a match, so a reference that starts using it, or a build that starts declaring it, fails the replay instead of passing quietly | `constants/walkSkipDirNames` in the `DIVERGENCES` ledger of `crates/vibe-cli/src/tui/completion/path/autocompletion_parity_tests.rs`, whose staleness check fails the suite once the case conforms |
| Playback degrades to a reported absent capability | The reference probes its output backend at construction and raises when none answers (`vibe/cli/audio_player/audio_player.py:31-42`). Neither a host without a backend nor one without an output device can be stood up in CI, so this port reports the absent capability instead of claiming coverage: the cause is carried as a typed variant rather than as message text, the narrated turn stays successful, and the operator is told once per session rather than once per turn. The absent-backend branch is the only one a test exercises, which is the residual the voice row above records | `PlaybackError::BackendUnavailable` in `crates/vibe-cli/src/tui/voice/player.rs` and `report_speech_failure` in `crates/vibe-cli/src/tui/mod.rs` |
| 5 configuration keys with no upstream counterpart | `thinking`, `notifications`, `proxy`, `tls_ca_path` and `dotenv_path` have no lossless reference target, and mapping them would reinterpret values already on disk | `crates/vibe-core/src/config/surface_parity_tests.rs`, which fails if the set changes |
| An unregistered key survives the merge | The reference merge drops a key its schema does not declare; keeping it lets a file written by a newer client round-trip through this one | `ConfigSnapshot::unregistered_keys`, asserted per corpus scenario |
| `config/batchWrite` | Writes several configuration targets in one request, which the reference client does with one `config/patch` per target. `vibe-cli` settings screens already depend on the atomic form; retiring it is its own migration | `LOCAL_EXTENSION_METHODS` in `crates/vibe-protocol/src/methods.rs`, kept out of `SERVER_METHODS` and out of `ServerCapabilities.methods`, asserted by `app_server_surface_parity_tests` |
| `connectors/toggle` | Enables or disables one connector without the full `config/patch` round trip the reference uses. Live callers in `vibe-cli` | Same as above |
| `mcp/auth/complete` | Completes the local MCP OAuth callback this port serves itself, which the reference delegates to its own browser flow | Same as above |
| `session/overrides/write` | Holds a model, a mode, a thinking level, a reasoning effort and an approval stance for one session's lifetime. Upstream none of the five is session-scoped: the model and the thinking level are configuration writes and the mode and the approval stance come from an agent profile. They used to ride on `session/settings/update`, which made that method accept five fields its reference model forbids; moving them here left it exactly `sessionId`, `maxTurns` and `maxTokens` | Same as above, plus `settings_update_is_strict_and_applies_to_the_next_turn_while_active`, which asserts each of the five is answered with `invalid_params` on the reference method |
