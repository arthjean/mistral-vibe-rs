# Parity Scorecard

Structural audit of Mistral Vibe RS against the upstream Python implementation.
Every axis carries a number, which is how to address one: axis 18 is hooks, axis
34 the conversation engine.

| Field | Value |
|---|---|
| First audit | 2026-08-04, Rust `5617d0c` |
| Last remeasure | 2026-09-25, the MCP pass (row 11, 78 to 100): three new differential oracles drive the reference's MCP client, its OAuth service and its `vibe-app-server` catalog against scripted MCP and authorization servers (`vibe-mcp-script-fixture`, `vibe-oauth-script-fixture`), and `crates/vibe-app-server/tests/mcp_transport_parity_tests.rs`, `mcp_oauth_parity_tests.rs` and `mcp_catalog_parity_tests.rs` replay them: 47/47, 49/49 and 15/15. The catalog moved onto the reference's contract: the seven `mcp_catalog/*` methods with their six `mcp/*` aliases, sessionless mutations against the user configuration, the `not_implemented` error code, `mcp_catalog/authUrl` and `mcp_catalog/authRequired`, and one multi-owner `McpAuthenticationService` behind the app server and `vibe mcp`. The local `mcp/auth/complete` extension is retired: a login answers once the browser comes back, and every client reads its URL live |
| Python reference | `4a96003`, reference package version 2.25.7, against this port's `[workspace.package] version` 2.24.0 |
| Reference checkout | `/home/arthur/dev/mistral-vibe` on Linux and `C:\dev\mistral-vibe` on Windows, relocatable through `VIBE_REFERENCE` and resolved by `vibe_core::parity::reference_root`. Every reference path in this document is relative to it and was resolved at the pin. Since v2.25.7 the checkout builds a compiled extension (`mistralai_vibe_local_harness._native`, through the maturin backend in `build_backend/maturin_backend.py`) that `vibe/cli/entrypoint.py` imports, so a checkout is only an oracle once `uv sync --frozen` has run in it at the pin |
| Pin sources | `vibe_core::parity::REFERENCE_COMMIT` and `EXPECTED_COMMIT` in `scripts/parity/pin.py`, held equal by `crates/vibe-core/src/parity/parity_tests.rs` |
| Restore an off-pin checkout | `git -C /home/arthur/dev/mistral-vibe checkout 4a96003186b166d55b9f06895c45bb136eef61cd` |
| Weighted score against the pin | **73.5/100**, from 4 927 over a weight of 67 (72.9 before the MCP pass, 71.5 before the LLM backends pass, 70.0 before the rewind pass, 68.6 before the ACP pass, 68.1 before the CLI surface pass, 66.7 before the managed shell pass, 66.1 before the worktree pass, 65.6 before the tool infrastructure pass, 64.6 before the built-in tools pass, 64.0 before the slash command pass, 63.9 before the distribution pass, 63.7 at the re-pin, 87.8 against the previous pin `b78b451`, 65 at first audit). The weighting is stated rather than judged, so the number recomputes from the table: weight 3 for a contract a session exercises continuously, axes 3, 6, 14, 16, 17, 30 and 34; weight 1 for a peripheral one, axes 1, 5, 21, 22, 23, 24, 25, 27, 32, 33, 35 and 36; weight 2 for the remaining 17. Without the two new axes the same rows give 75.8, from 4 927 over 65 |
| Weighted score against upstream `HEAD` | Equal to the pinned score on 2026-09-23: the pin is upstream `HEAD` (`4a96003`, v2.25.7, committed 2026-09-22). It drifts again with the next upstream release |

## Two targets

Every score in this document answers one of two questions, and between re-pins
they stop giving the same answer.

**Against the pin** is what the oracles measure. `4a96003` is the commit every
committed corpus was captured from, so a replay is only an oracle for that
revision. This is the number that moves when work lands here.

**Against upstream `HEAD`** is what a user comparing the two products
experiences. On the day of this re-pin the two coincide. The previous pin,
v2.24.0, had fallen ten releases behind; the section below records what those
releases moved, so the drop from 87.8 to 63.7 can be read rather than trusted.

Keeping both numbers is deliberate. A port that reaches 100 against a pin it
never advances has not reached parity with anything a user runs. Re-pinning is
the only way to close the second column, and `AGENTS.md` requires regenerating
every committed corpus in the same change, which this pass did.

## Method

Scores measure **reproduced surface**, not behavioral conformance. They come from
diffing inventories, not from running both implementations side by side:

- app-server JSON-RPC method names (136 upstream `SERVER_METHODS` plus 7 `clientTool/*`, counted from the pinned checkout by `scripts/parity/app_server_surface.py`);
- user-facing configuration keys (`vibe/core/config/vibe_schema.py` against
  `LayeredConfig::schema()`);
- published agent tool names and schemas;
- slash commands and CLI flags;
- module presence and depth, measured in lines of production code.

A part scores 100 when its externally observable contract is fully reproduced.
Anything measured only by module presence carries more uncertainty than anything
measured by name-level diff, and the table notes which is which.

Module presence is the weakest evidence there is, and the 2026-08-19 audit
showed why: every subsystem in this port was checked for whether anything
outside its own module and outside test code constructs it, and hooks was the
one that nothing does. A row that says a mechanism is reproduced now means it
runs, not that it compiles. Rows carrying **Measured by a differential oracle**
are replayed against a committed corpus; the others are inventory diffs or
structural judgements, and say so in place.

Every row of the parity table names the reference tree it was measured against,
so a reader can open the same files the score came from. Those paths are
relative to the checkout root recorded in the header and are the state of the
reference at the pin. Citing them rather than quoting them is what `NOTICE`
requires.

Where a differential oracle exists, the score comes from running it rather than
from reading source, and the row says so and carries the figure the replay
prints. Thirty-two modules matching `*_parity_tests.rs` hold those replays,
against corpora captured by the twenty-six capture scripts under
`scripts/parity/` and the seven runtime oracles under
`crates/vibe-cli/tests/runtime-parity/`. The capture scripts sit beside three
shared helpers, `harness.py`, `pin.py` and `scenarios.py`, that capture nothing
themselves. Four of them read the tool surface from a different angle:
`tool_surface.py` for the published names and schemas, `tool_config.py` for
the per-tool configuration keys, `tool_execution.py` for what a call returns
and `tool_presentation.py` for how a call and its result are shown, the last
of which drives the reference's own `ToolUIDataAdapter`. Every one of them
replays unconditionally; only the probe that recaptures from the pinned
checkout skips, so a missing or off-pin reference never fails `cargo test`.
Both counts were recounted 2026-09-24 by listing the files: `ls
scripts/parity/*.py` answers 29, of which the three helpers are not captures,
and `find crates -name '*_parity_tests.rs'` answers 32, the last of them
`crates/vibe-app-server/tests/rewind_parity_tests.rs`, added by the rewind pass. A corpus is committed
on the terms `NOTICE` sets: names, pointers, verdicts and counts, with every
reference-authored sentence reduced to a length and a SHA-256. Each replay also
carries a ledger of what is still divergent, and fails both on a divergence no
entry names and on an entry whose divergence has stopped reproducing, which is
what keeps a score from drifting away from the code under it. The re-pin
exercised that second half: where the reference converged on this port, 26
managed shell entries, three shell grants and one probe of a retired method went
stale and were removed rather than kept.

Twenty-four of the thirty capture scripts changed to run at `4a96003`, one of them
in its docstring only, together with the helpers `harness.py` and `scenarios.py`,
each following a reference module that moved, a signature that changed or a
symbol that was removed, and none adding a family: the app-server census follows
the 136-method inventory, the command and composer captures follow the
`CommandContext` that replaced the Vibe Code gate, the permission and shell
captures pass a `Workspace` where they passed a working directory, the worktree
capture follows `vibe/core/worktree.py` into `vibe/core/git/`, the voice
capture follows the move from sounddevice to miniaudio, the onboarding capture
stubs the new `whoami` tenant discovery to its no-data branch, and the CLI
surface oracle puts the harness runtime sources on its path. Six needed nothing
but the recapture.

### Measured volumes

| Tree | Lines |
|---|---|
| `vibe/` at the pin (Python, tests live in `tests/`) | 156 214 across 590 files, against 92 207 across 475 at `b78b451` |
| `vibe/cli-rust/` at the pin (Rust, new at v2.25.7) | 50 495 across 321 files |
| `harness/` at the pin (Rust core and its Python runtime, new at v2.25.7) | 45 862 in `harness/core` across 135 `.rs` files, 42 833 in `harness/runtimes` across 82 `.py` files |
| `crates/` total | 251 669 across 520 files |
| `crates/` production, inline `#[cfg(test)]` blocks removed | not remeasured on 2026-09-23; roughly 124 200 on 2026-08-19 |

Remeasured 2026-09-23 with `git ls-tree` and `wc -l` over the pinned tree.
Volume is context, never a score.

## Parity by part

| # | Part | Score | Reference | State and gaps |
|---|---|---|---|---|
| 1 | Distribution, updates, installers | 100 | `vibe/cli/update_notifier/`, `vibe/utils/cache_store.py`, `vibe/utils/http.py`, `vibe/cli/launcher.py`, `build_backend/maturin_backend.py`, `pyinstaller/`, `vibe.spec`, `vibe-acp.spec`, `vibe-app-server.spec`, `action.yml`, `.github/workflows/build-and-upload.yml` | Oracle `vibe-cli terminal_services`: the nine update traces replay 130 events against the pinned `packaging` 26.2, `FileSystemUpdateCacheRepository` and both gateways, and differ from the reference on exactly the seven events their ledgers name, all decided under Accepted divergences. `vibe update` is `--check-upgrade` on the first argument only (`crates/vibe-cli/src/argv.rs`, six `update` vectors in `cli_surface_parity`). `--check-upgrade` validates `--workdir` and `--add-dir` as every route does, loads the configuration and exits 1 on one no session could load, then paints the prompt with the configured theme, as the startup prompt now does too (`crates/vibe-cli/tests/update_routes.rs`). `Version` is the whole PEP 440 grammar with `str()` normalization and the `_cmpkey` order, digits of any width included (`crates/vibe-core/src/updates/version.rs`). The cache store reads the disk once per repository, remembers what it is given, logs and ignores a failed write, reads a file of any size, and one store is threaded through a launch. Both gateways read the body before the status, follow no redirect, time out per phase, and trust `SSL_CERT_FILE` and `SSL_CERT_DIR` in addition to the system store (`crates/vibe-core/src/http_trust.rs`); PyPI reports the normalized version and iterates `versions` and `files` as Python does, GitHub reads `prerelease` and `draft` for truthiness. A failed background check is logged as a warning. The action declares the four reference inputs with their requirements and defaults and runs `vibe -p` for text (`action_parity_tests`). The Linux legs build in the reference's manylinux_2_28 images and `scripts/ci/check-linux-portability.sh` holds both binaries to glibc 2.28 |
| 2 | Slash commands | 100 | `vibe/cli/commands.py`, `vibe/cli/autocompletion/slash_command.py`, `vibe/cli/autocompletion/completers.py`, `vibe/cli/textual_ui/widgets/chat_input/input_kinds.py`, `vibe/cli/textual_ui/app.py` | Oracle `vibe-cli commands_parity`: 704/733 comparisons over 13 families, the 29 others accepted. The registry answers the reference's 34 keys and 41 aliases under every context; `traits` 68/68, `dispatch` 56/56 over six composer states, `handlers` 118/121 over every command and branch, `logLevelApply` 10/10 and `logLevelPicker` 33/33. What differs is text `NOTICE` forbids shipping: the thirteen authored help lines, the `/help` message that carries them, and the continuation `/retry` submits, all under Accepted divergences. Composer corpus: no slash trace diverges. The panels a command opens are measured by their own rows (14, 28) |
| 3 | Built-in tools | 100 | `vibe/core/tools/builtins/`, `vibe/core/tools/builtins/prompts/`, `vibe/questions.py`, `vibe/core/subagents.py` | Oracles over surface, execution and conversion: 12/12 schemas (16/16 managed), 93/93 argument fixtures, 478/478 captured pages converted to the reference's Markdown, and 85/103 execution cases over 11 tools, the other 18 differing only in the sentences `NOTICE` forbids copying (51 `LICENSING` entries, no recorded decision left). The built-in tools pass closed the rest: `web_fetch` converts HTML the way `markdownify` does over a port of the `html.parser` tree, sends the reference's request envelope, decodes its content encodings and scopes an approval to the origin; the file tools read a path argument as `ToolPath` does and reach an approved path outside the root; `grep` drops matches in sensitive files; `skill` honors `disable-model-invocation` and samples a skill's files recursively; and `task` joins every assistant message of the child run |
| 4 | Tool infrastructure (registry, schemas, filtering) | 100 | `vibe/core/tools/base.py`, `vibe/core/tools/manager.py`, `vibe/core/tools/utils.py`, `vibe/core/tools/permissions.py`, `vibe/core/tools/ui.py`, `vibe/utils/tool_presentation.py`, `vibe/core/workspace.py` | Oracles over presentation (58/132 cases, 223 tolerated fields over 183 entries, 138 recorded decisions and 85 held by `NOTICE`), configuration (146/146 pairs), descriptions (7/7) and the permission surface (0 ledgered). File displays are cwd-relative with the scratchpad suffix, measured over 8 path shapes, results carry `approvalNote`, the effect vocabulary has the 14 kinds, `tool/list` publishes `isCustom`, a literal requirement is covered only by its own text, and a tool reaches the session's authorized roots, its working directory and added directories, with trust never refusing a path. Every remaining difference is under Accepted divergences |
| 5 | Worktree (`--worktree`) | 100 | `vibe/core/git/worktree/`, `vibe/core/git/repo.py`, `vibe/core/git/errors.py`, `vibe/core/session/worktrees.py`, `vibe/app_server/_worktree_session.py`, `vibe/cli/entrypoint.py` | Oracle `vibe-core worktree_parity`: 138/138 over 14 families and 9 scripted repositories, no ledger, live probe matching. `cli_surface_parity` accepts the bare flag; the listing and removal answers validate against the census. Held by three accepted divergences: plain lifecycle lines, no `chdir`, and the naming prompt's own prose |
| 6 | Managed shell and terminals | 100 | `vibe/core/tools/builtins/managed_shell/`, `vibe/core/tools/builtins/experimental_bash.py`, `vibe/core/tools/builtins/_shell_command_policy.py`, `vibe/core/tools/builtins/_shell_permission_analysis.py`, `vibe/core/tools/builtins/windows_shell.py`, `vibe/core/tools/builtins/git_bash.py`, `vibe/core/tools/builtins/bash.py`, `vibe/core/tools/terminal_runtime.py` | Oracle `vibe-core session_parity`: 75/88 sessions byte-conformant, the other 13 differing only by the 19 entries of the two accepted shell rows (the refusal kind and the action sentences), surface 4/4 quadrants. Oracle `vibe-core shell_parity`: 132/132 legacy resolutions, 16/16 managed, 40/40 against git repository configuration, 21/21 `*_stdin` permissions, 47/47 extractions, 23/23 escaping operands, 45/45 path-inspecting commands and the PowerShell grammar (33 commands, 8 patterns, 13 expansions, 16 tokens), with only the 16 approval-label fields the licensing row keeps |
| 7 | CLI surface (flags, modes) | 100 | `vibe/cli/entrypoint.py`, `vibe/cli/cli.py`, `vibe/cli/mcp_command.py`, `vibe/cli/launcher.py`, `vibe/_experimental_harness.py` | Oracle `vibe-cli cli_surface_parity`: 186 of 187 argv cases, 43 actions over 4 parsers, 11 ledger entries, all accepted, the live probe matching; the one other case differs only in the help prose `NOTICE` keeps out. Both parsers read a vector as argparse does before clap sees it (`crates/vibe-cli/src/argv/reading.rs`): abbreviations with the ambiguity refusal, hyphen-led values and positionals, the last occurrence winning, a flag given `=value` refused; `--max-turns`, `--max-tokens`, `--max-price` and the two `mcp add` timeouts convert as `int()` and `float()`. `--experimental-harness` and `--legacy-harness` are one exclusive pair and `--smart-approve` is rewritten after the parse; a request for the Unified Harness lands on the legacy one with the reference's own fallback, `startupIssue` and `harnessSelectionSource` on `config/read` and a TUI notice, and `--smart-approve` starts under the `smart-approve` profile. `vibe mcp add --allow-insecure-http` stores a plaintext URL for another host, which a session then connects to. The budgets are `-p` options only, and a budget already spent ends the run before its first request. Held by six accepted divergences: the nine port-only arguments, the help prose, the Unified Harness fallback, 64-bit budgets, the PTY helper entry and the process name |
| 8 | ACP (`vibe-acp`) | 100 | `vibe/acp/`, `vibe/acp/commands/`, `vibe/acp/tools/`, `vibe/acp/voice.py` | **Measured by a differential oracle.** `scripts/parity/acp.py` runs 79 stdio scenarios and 23 argument vectors against both binaries, and `crates/vibe-acp/tests/acp_parity_tests.rs` replays them: 23/23 vectors and 75/79 scenarios conformant. The four others are ledgered there: the terminal sign-in launch is an accepted divergence, and three relay another part's contract unchanged, the attachment path under the session layout (row 16), `_config/schema` (row 30) and `_projectLinks/*` (row 25). Every standard method, the eleven commands, question elicitation and every extension the reference's agent routes are served |
| 9 | Rewind | 100 | `vibe/core/rewind/`, `vibe/app_server/_projection.py` | **Measured by a differential oracle.** `scripts/parity/rewind.py` over 12 scenarios: the read of each point, the refusals of both methods, forks and in-place rewinds with and without restoring files, to the first message, twice in a row, while a turn runs, and across and after a compaction. 5/12 scenarios conformant; the other seven differ only in 44 fields of entries a rewind keeps, ledgered to the rows that own them: the session directory layout (16), the call display's content preview (4) and approval provenance on a settled effect (34). The terminal panel asks for the edit action and then for the persistence choice, `Esc` stepping back, which the US-029 runtime trace replays |
| 10 | LLM backends | 100 | `vibe/core/llm/`, `vibe/core/llm/backend/` | Oracle: `scripts/parity/llm_backends.py`, replayed by `crates/vibe-core/src/llm/backend_parity_tests.rs` at 358/358 scenarios conformant (128 wire, 110 error, 62 parse and 58 retry) with an empty ledger, plus 32 recorded backoff delays, 6 utility model selections and 6 Vertex endpoints; the live probe matches a fresh capture. The failure sentences are this port's own, held to digest inequality (accepted divergences below) |
| 11 | MCP | 100 | `vibe/core/tools/mcp/`, `vibe/core/auth/mcp_oauth.py`, `vibe/core/config/mcp_servers.py`, `vibe/cli/mcp_command.py`, `vibe/app_server/mcp_catalog.py` | Four oracles. Configuration 22/22 entries and 22 URL scenarios. Transport (`scripts/parity/mcp_transport.py`, the SDK client over stdio, HTTP and streamable HTTP against `vibe-mcp-script-fixture`): 47/47 conformant, one ledgered divergence. OAuth (`scripts/parity/mcp_oauth.py`, `MCPAuthenticationService` against `vibe-oauth-script-fixture`): 49/49, two ledgered header divergences and this port's own callback pages. Catalog (`scripts/parity/mcp_catalog.py`, the reference `vibe-app-server` against the same fixture over a shared file keyring): 15/15 conformant with an empty ledger, covering the seven `mcp_catalog/*` methods and their `mcp/*` aliases, sessionless mutations, targeting, validation, `mcp_catalog/authUrl`, `mcp/authUrl` and `mcp_catalog/authRequired`, credential removal and stored credentials. `vibe mcp`, the terminal client and the ACP agent sign in through the same blocking login, the URL reaching the operator as it is published |
| 12 | Trusted folders and permissions | 90 | `vibe/core/trusted_folders.py`, `vibe/permissions.py`, `vibe/core/tools/permissions.py`, `vibe/core/tools/utils.py` | Oracle over scopes 4/4, arity 138/138, session patterns 20/20 and sensitive patterns 20/20 and the sensitive chain 6/6, 0 ledgered: `RequiredPermission.literal` converged and a declined folder is asked about rather than refused, as `Workspace.allows` reads it. Gaps: `workspace/trust/untrustedConfig` and the owner-only trust file |
| 13 | Programmatic mode (`-p`) | 80 | `vibe/cli/programmatic.py`, `vibe/cli/cli.py`, `vibe/cli/entrypoint.py` | text, json and streaming implemented, the launch headless. Divergences: the default agent is `accept-edits` upstream, so a bare `vibe -p` applies edits there and denies them here; `--auto-approve` without `--agent` now selects the `auto-approve` profile; the harness flags and `--smart-approve` run on the legacy harness (row 7) |
| 14 | TUI (composer, transcript, pickers) | 55 | `vibe/cli/textual_ui/`, `vibe/cli/textual_ui/widgets/`, `vibe/cli/textual_ui/screens/`, `vibe/cli/textual_ui/app.py` | Composer corpus: 103 traces, 270 parity dimensions and 44 gaps over 44 ledger entries. Runtime corpora: semantic transcript 32/35 events, active turn 5 traces plus 1 unavailable. Gaps: the server-side turn queue, bare-`@` listing, grouped file effects, the tab-state title, vim navigation, `load_more` |
| 15 | Review and turn diff | 95 | `vibe/core/review/`, `vibe/app_server/review.py`, `vibe/app_server/_review.py` | Checkpoint oracle plus census: 73/73 scenarios, and the review files are byte-identical between the pins. The log a compaction used to empty is now kept, which the rewind oracle measures through the restore plans it reads across and after a compaction. Residual: no oracle replays `review/*` over a compacted session |
| 16 | Sessions, resume, fork, history | 50 | `vibe/core/session/`, `vibe/core/types.py`, `vibe/app_server/_projection.py` | Layout, locks, pagination and migration as before. Gaps: `session/list` reshaped, five new persisted `meta.json` keys this port drops on rewrite, the session lease, `session/pin`, `session/relocate`, `session/history/*` and `session/turns/list` |
| 17 | app-server protocol | 50 | `vibe/app_server/`, `vibe/app_server/protocol.py`, `vibe/app_server/models.py`, `vibe/app_server/_connection_protocol.py` | Oracle: 88/136 methods declared (48 undeclared, 6 this port serves retired upstream), 87/136 routed, 15/22 notifications, 12/15 error codes, 494 models, 22/25 probed responses validate. Details in the section below |
| 18 | Hooks | 20 | `vibe/core/hooks/`, `vibe/core/agent_loop_hooks.py` | **No production path runs a hook**, as before. The reference moved further: plugin-contributed hooks, `hooksCount` on `config/read`, and approval provenance on skipped tools. Two open divergences |
| 19 | System prompt and project context | 40 | `vibe/core/system_prompt.py`, `vibe/core/prompts/`, `vibe/core/config/harness_files/` | `AGENTS.md` walk-up and prompt resolution implemented. Gaps: four `include_*` keys and `system_prompt_id` unread, project context no longer runs `git status` upstream, `cli_2026-08_v3`, the model-invocable skill section, and the project source disabled when the cwd is the vibe home |
| 20 | Agents, subagents, delegation | 55 | `vibe/core/agents/`, `vibe/core/subagents.py`, `vibe/agents.py` | Profiles, registry, subagents and a published `task`. Gaps: `default` renamed `ask` and `accept-edits` made the default at v2.24.1, the `RENAMED_AGENTS` migration, the smart-approve rollout gate (`smart_approve_available` and `smart_approve_default`, row 30) and the offer the reference keeps for the rest of a session that switched away from it (the profile and the `smart` safety exist since the CLI surface pass, offered only to the session running it, and its classifier belongs to the Unified Harness, row 36), the rest of the `task` result (no progress event per child tool result, and a failed child reports its error instead of appending it to what it had answered), and the profile `exit_plan_mode` switches to, which the reference's loop applies within the turn and this port leaves to the terminal client for the next one |
| 21 | Connectors | 40 | `vibe/core/tools/connectors/`, `vibe/app_server/connector_catalog.py` | Registry, auth and refresh as before. Gaps: the four `connector_catalog/*` methods and three notifications, display names, bootstrap diagnostics, the nullable total, and the `web_search` bootstrap query |
| 22 | Teleport and Vibe Code Web | 77 | `vibe/core/teleport/`, `vibe/app_server/_vibe_code.py`, `vibe/core/git/remote.py`, `vibe/core/git/fetch.py` | Teleport and projects on one store. Divergences: `vibe_code_enabled` and `vibe_code_api_key_env_var` were removed at v2.25.7 and still gate teleport here, the credential now comes from the Mistral provider, eligibility changed, and the hardened fetch |
| 23 | Autocompletion | 55 | `vibe/core/autocompletion/`, `vibe/cli/autocompletion/` | Oracle: 157/159 comparisons over 5 families, 2 ledgered. The v2.24.5 fuzzy scorer is ported and all 40 ranking cases conform; a differential sweep of 19 689 random pattern and text pairs, Unicode included, matches the reference score for score. The processing cap was removed at v2.25.3 |
| 24 | Voice (STT, TTS, narrator) | 75 | `vibe/cli/voice_manager/`, `vibe/cli/narrator_manager/`, `vibe/cli/transcribe/`, `vibe/cli/tts/`, `vibe/cli/audio_player/`, `vibe/cli/audio_recorder/`, `vibe/acp/voice.py` | Oracle: 1 011/1 064 comparisons over 4 families, 8 ledgered; narrator traces unchanged. Unmeasured: the move to miniaudio, the no-speech and no-audio outcomes, the ACP voice bridge |
| 25 | Vibe Code Project | 55 | `vibe/core/vibe_code_project/`, `vibe/app_server/_project_links.py` | The `projectLinks` wire was rebuilt around directories at v2.25.1: 1 of 9 answers validates against the census (`unlink`), 33 recorded issues. Local links in `projects.toml` absent |
| 26 | Compaction | 100 | `vibe/core/compaction/`, `vibe/core/middleware.py` | Oracle: 172/172 comparisons over 8 families, four ledgered: three licensing entries and one telemetry call type no census carries. A compaction appends its `context_boundary` envelope and keeps every earlier message and the session identity, and the model is sent only what follows the latest envelope, which the rewind oracle's two compaction scenarios also measure end to end |
| 27 | Telemetry and observability | 76 | `vibe/core/telemetry/`, `vibe/core/tracing.py`, `vibe/observability/`, `vibe/core/log_reader.py` | Oracle: 983/1 091 comparisons over 15 families, 96 ledgered. Gaps: `arch`, `experiment_assignments` and four base fields, `host_kind`, the `desktop` entrypoint, `approval_source` |
| 28 | Skills | 55 | `vibe/core/skills/`, `vibe/core/skills/builtins/`, `vibe/core/skills/registry/`, `vibe/app_server/_skills_service.py` | Oracle: 209 compared cases, 182 conforming and 27 ledgered; `disable-model-invocation` and the `agents/openai.yaml` policy conform since the built-in tools pass. Gaps: per-root scope, the `SkillSummary` fields, and the registry, which v2.25.x wired into twelve `skills/*` methods |
| 29 | Checkpoints | 100 | `vibe/core/checkpoints/` | Oracle: 32/32 line and 36/36 opcode fixtures plus 73 engine scenarios over 7 families, empty ledger. The log survives a compaction as upstream, measured by the rewind oracle restoring files across one. Residual: the retention ceiling below |
| 30 | Configuration | 70 | `vibe/core/config/`, `vibe/core/config/layers/`, `vibe/core/config/vibe_schema.py`, `vibe/config_values.py` | Oracle: 70/78 fields declared, 29/30 merge and 10/11 model scenarios, 4 default pointers diverging. Gaps: 8 undeclared fields, the shallow `merge` strategy on four fields, and an unknown `active_model` reset to empty |
| 31 | Setup, onboarding, authentication | 82 | `vibe/setup/`, `vibe/core/auth/`, `vibe/setup/auth/whoami.py` | Two oracles: 182 authentication comparisons, empty ledger, and 517 onboarding comparisons, 1 ledgered. Unmeasured gaps: the `whoami` client and its cache, tenant discovery, the split-horizon API base and `browser_auth_allow_origin_rewrite` |
| 32 | Experiments and rollouts | 50 | `vibe/core/experiments/`, `vibe/core/config/layers/growthbook.py` | Oracle: 411/502 comparisons over 13 families, 91 ledgered. Gaps: five experiment names, typed defaults, the 10 s identity timeout, four field mappings, assignment records and the new eval attributes |
| 33 | VS Code extension promo | 10 | `vibe/cli/vscode_extension_promo/` | Not ported. The subtree is byte-identical between the pins; oracle 1/64, 9 ledgered |
| 34 | Conversation engine (agent loop) | 60 | `vibe/core/agent_loop/`, `vibe/core/types.py`, `vibe/core/middleware.py`, `vibe/core/plan_session.py` | No oracle compares the two loops. Gaps: approval provenance on settled tools, split failure text, background session titles, relocation and the session lease |
| 35 | Plugins | 0 | `vibe/core/plugins/`, `vibe/plugins/`, `vibe/app_server/_plugins.py` | Not ported. New since v2.24.4: plugin discovery in `$VIBE_HOME/plugins` and `.vibe/plugins`, native and foreign manifests, `plugin/*` and `plugins/read` methods, `/plugins` and `/reload-plugins`. Upstream resolves plugins only under the Unified backend |
| 36 | Unified harness backend | 0 | `vibe/_experimental_harness.py`, `vibe/app_server/_unified_harness_backend_adapter.py`, `vibe/app_server/_session_backend_port.py`, `harness/` | Not ported. A Rust core behind the Python app server, selected by `--experimental-harness`, `--legacy-harness` or the `vibe_cli_unified_harness_rollout` key, default `legacy`. Features that land only there: smart approve, todos, vision fallback, child sessions |

Each cell states in one line what the score rests on and what holds it back.
The detail is not repeated there: the divergences are named row by row in the
two tables at the end of this document, and an oracle is reproduced with `cargo
test -p <crate> --all-features <filter> -- --nocapture` over the parity module
the part owns, with `FORCE_COLOR` unset and `VIBE_HOME` pointed at an empty
directory.

## What the re-pin moved: v2.24.0 to v2.25.7

Measured 2026-09-23 between `b78b451` and `4a96003`. Eleven releases separate
them (v2.24.1 to v2.24.5, v2.25.0 to v2.25.7; v2.25.6 has no commit of its own
and ships inside v2.25.7). `git diff --shortstat` over `vibe/` answers 733 files,
+129 515 / -8 198; without the new `vibe/cli-rust/` it is 406 files, +74 541 /
-8 198. The app server took the largest share, +32 509 / -2 393 over 82 files,
of which `vibe/app_server/_unified_harness_backend_adapter.py` alone is 8 502
new lines.

**The app-server surface** grew from 91 to 136 declared methods, +52 and -7.
The seven retired names are `callback/respond`, `config/patch`,
`config/thinking/write`, `history/list`, `session/close`,
`session/compact/start` and `workspace/worktrees/list`, and this port served
all seven until the worktree pass retired `workspace/worktrees/list` for its
successor. Their successors, `callback/result`, `config/write`,
`session/history/list`, `session/stop`, `session/compact`,
`workspace/git/worktrees/list` and `config/model/write`, are among the 52 it
does not declare, with the `mcp_catalog/*`, `connector_catalog/*`, `skills/*`,
`plugin/*`, `session/turn/*` queue and `workspace/git/*` families. The earlier
drift note said `shell/run`, `shell/interrupt` and `session/title/update` had
been renamed away; v2.25.0 declared all three again, and at the pin no
dispatcher reads them under those names. Notifications grew from 15 to 22,
error codes from 12 to 15, the model census from 373 to 494. An editor written
against today's `vibe/app_server/protocol.py` fails here on method-not-found
for most of the configuration, MCP, skills and queue lifecycle.

**The command and flag surface** grew by six slash commands (row 2), five
flags and a `vibe update` alias (rows 1 and 7; the alias was ported by the
distribution pass of 2026-09-23, and the flags by the CLI surface pass of
2026-09-24). The default agent profile was
renamed from `default` to `ask` and the default stance moved to
`accept-edits` at v2.24.1, which changes what a bare `vibe -p` does (row 13).

**Behavior this port was measured as matching and no longer does**: compaction
appends a `context_boundary` envelope instead of resetting the history (rows
26, 29, 34, 15 and 9), the fuzzy scorer was rewritten (rows 2, 14 and 23), file
displays became cwd-relative (rows 4 and 14), `web_fetch` follows redirects by
hand (row 3), the sensitive-file grant became per file and its default list
grew (rows 4 and 12), the terminal title shows a tab-state indicator (row 14),
and the GrowthBook layer, its attributes and its assignment records were
reworked (row 32).

**Where the reference converged on this port**: the managed shell keeps CRLF
since v2.24.2, and the reference now asks on the three shell commands this
port used to be alone in asking about. Their ledger entries were removed.

**The upstream move to Rust.** This is context rather than parity, and it is
the most consequential thing in the diff. `vibe/_experimental_harness.py`
appeared at v2.24.2, not v2.24.1 as this document used to say, and its module
was renamed from `mistralai_rust_harness` to `mistralai_vibe_local_harness` at
v2.25.0. v2.25.7 lands the harness sources (`harness/core`, a Rust crate built
into the wheel as a Python extension), a Rust terminal client
(`vibe/cli-rust`, binary `vibe-rs`, launched only when `VIBE_CLI=rust` through
`vibe/cli/launcher.py`) and a maturin build backend. Neither is the default:
the Python terminal client remains the `vibe` entry, and the Unified backend is
selected by flag or rollout with `legacy` as the default variant. But upstream
now lands new features on the Unified backend only (row 36), while this port
mirrors the legacy backend's contracts. The upstream Rust sources declare a
proprietary license, which puts them further out of reach than the Python ones
under `NOTICE`.

## Open divergences

Accepted divergences below are decided. These are not: each is a place this port
answers something the reference does not, and each is intended to close. Every
row names the reference file that publishes the contract, the file here that
breaks it, and, where a test tolerates the difference until it closes, the test
that fails the day it stops reproducing. The first six were found by the
2026-08-19 re-read, and the rows from the compaction row down were added by the v2.25.7 re-pin.
The managed shell pass of 2026-09-23 closed the eight managed shell rows, and
the ACP pass of 2026-09-24 closed `ACP serves 8 of 11 commands`.
The re-pin added far more divergences than this table lists: each replay module
names its own entries, and the rows below are the ones a reader has to know
about or that a test looks up by name.

| Part | What diverges | Reference | Here |
|---|---|---|---|
| No production path runs a hook | `HookManager` is constructed three times in this repository, all three inside the `#[cfg(test)]` block of its own crate, and no other module calls its chain. `hooks.toml` is discovered, validated and counted, and never executed. This is the divergence that has to close before the payload one below can matter to anyone | `vibe/core/hooks/manager.py`, `vibe/core/agent_loop_hooks.py` | `HookManager` in `crates/vibe-core/src/extensions/hooks.rs`, called from nowhere |
| The hook invocation written to stdin | The reference writes `invocation.model_dump_json()`, so a hook reads thirteen snake_case fields. This port writes seven in camelCase: four are the same name in another case, `hook_event_name` becomes `kind` with the reference's own values, `tool_input` becomes `payload`, `tool_output_text` becomes `outputText`, and `transcript_path`, `cwd`, `tool_status`, `tool_output`, `tool_error` and `duration_ms` are absent. Since v2.25.x the `bash` tool output a post-tool hook reads is a `CapturedShellResult`. Reading the hook's answer is conformant, so the divergence is one-directional | `vibe/core/hooks/models.py`, `vibe/core/hooks/executor.py` | `HookInvocation` in `crates/vibe-core/src/extensions/hooks.rs` |
| 8 configuration fields are undeclared | `show_greeting` (v2.24.0), `allowed_models` (v2.24.1), `routed_extra_models` (v2.24.5), `smart_approve_available`, `smart_approve_default` and `experimental_enable_tab_status` (v2.25.1), `show_subagent_status_list` (v2.25.5) and `vision_model` (v2.25.7). Several also travel in the projected configuration view, so answered methods are short fields rather than only the file being short keys | `vibe/core/config/vibe_schema.py`, `vibe/app_server/config.py` | `UNDECLARED_FIELDS` in `crates/vibe-core/src/config/surface_parity_tests.rs` |
| Four `include_*` keys are declared and unread | `include_commit_signature`, `include_model_info`, `include_project_context` and `include_prompt_detail` gate four sections of the reference system prompt (`vibe/core/system_prompt.py:398-417`). Each is declared in the registry here and read nowhere, so setting any of them changes no prompt | `vibe/core/system_prompt.py` | `crates/vibe-core/src/config/registry.rs` |
| The remote skill registry is wired upstream | The former accepted row said the reference shipped its registry client dormant. v2.25.x wired it: `vibe/core/skills/manager.py`, the agent loop and `vibe/app_server/_skills_service.py` import it, and twelve `skills/*` methods publish it. This port still reads the registry only as a dormant subtree | `vibe/core/skills/registry/_service.py`, `vibe/app_server/protocol.py:198-210` | `published_skills` in `crates/vibe-core/src/skills/registry.rs`, and the `skills/*` entries of `UNDECLARED_METHODS` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs` |
| An unknown `active_model` keeps its pin | Since v2.25.0 the reference resets an `active_model` that names no configured model to the empty string, which resolves on read; this port pins `mistral-medium-3.5`. One model scenario, `models-unknown-active-model-falls-back`, diverges through it | `vibe/core/config/vibe_schema.py:916` | The model entries of `crates/vibe-core/src/config/surface_parity_tests.rs` |
| The profile `exit_plan_mode` asks for is applied by the client | Reference `ExitPlanMode.run` hands its target, `accept-edits` for either auto-approve choice and `ask` for manual approval, to `AgentLoop.switch_agent`, which reloads the loop within the running turn, so the model's next request already runs under the new profile. This port answers the plan review with the same `switched` verdict and leaves the switch to the terminal client, which moves to code mode for the next turn, so an app-server client that does not switch stays in plan mode. It closes with the `ask` profile of row 20 | `vibe/core/tools/builtins/exit_plan_mode.py:145-148`, `vibe/core/agent_loop/_loop.py:3646` | `run_interactive_plan_review` in `crates/vibe-app-server/src/client/interactive.rs`, and the plan transition in `crates/vibe-cli/src/tui/callback.rs` |

## Accepted divergences

Some parts cannot reach 100 through code alone. For these, 100 means a decided
and documented divergence, not a port. Recording the decision early keeps it out
of every subsequent parity review. Each row names why the divergence stands and
what in the repository holds it in place.

The v2.25.7 re-pin added three rows here that nothing had decided, because the
tool replays can only point a ledger entry at this table. The built-in tools
pass of 2026-09-23 closed `A redirected fetch reports the requested URL`, and
the tool infrastructure pass of the same day closed the other two, `A file
display names the file, not its cwd-relative path` and `No result display
carries an approval note`.

| Part | Reason | Evidence |
|---|---|---|
| Tool description text | `NOTICE` forbids shipping upstream prose, so the 13.9 KB of reference directive text (13 853 bytes at the pin) has no byte-identical counterpart here and never will. Descriptions are held to directive coverage and compared for presence only. **This does not cap the score in this document**, measured 2026-08-06 and settled two ways. By stated method: the scoring unit above is names and schemas, and description text is not among the inventories it diffs. By construction: the instrument that produces the tool scores replaces every description with `<described>` in both the capture and the replay, so prose carries exactly zero weight and no amount of it can move the number. The residue is therefore a licensing fact with no scoring consequence here | `DESCRIBED` in `scripts/parity/tool_surface.py` and `crates/vibe-app-server/src/tool_surface_parity_tests.rs`, which substitute every description before diffing, plus the `## Method` section above |
| A third-party score weighing description text | Not determinable from this repository, and recorded as such rather than left open. The externally reported 35/100 score weighs names and schemas "in an unpublished way", so no method exists here to confirm or refute against. The exposure is bounded instead of guessed: measured at the previous pin `b78b451`, the reference published 13 776 bytes of tool prompt prose and 1 735 bytes of parameter descriptions against 3 789 bytes of description-free schema and 94 bytes of names. At `4a96003` the prompt files total 13 853 bytes, five shell description files having changed, and the three other figures were not remeasured. A byte-weighing external metric would therefore hold roughly four fifths of the tool contract permanently out of reach, which would make that metric a measure of licensing posture rather than of engineering parity. Chasing it is refused on that basis | `vibe/core/tools/builtins/prompts/*.md` at `b78b451`, byte-counted out of tree; no prose is stored here |
| `grep` returns its matches sorted | `rg` walks in parallel and prints whichever file finished first, so its match order is not a contract anything can conform to: one capture of the pinned reference recorded two different orders for the same query in a single run. This port sorts by path and then by line, and the capture script sorts the reference's output the same way before recording it, so the match *set* is still compared byte for byte while the order is normalized out on both sides | `stabilize` in `scripts/parity/tool_execution.py` and the sort in `crates/vibe-core/src/workspace/search.rs`, replayed by `tool_execution_parity_tests`; the ACP capture orders a grep call's locations and matches the same way in `sort_grep_matches` of `scripts/parity/acp.py` |
| Authored result and error text in six tools | `NOTICE` forbids shipping upstream prose and the PRD lists byte-identical message text as a non-goal, so every sentence a result carries is this port's own, held to naming the same cause, value and limit rather than the reference's words. Six tools carry one, each scoped to one field of one result: `read_file` answers an empty file and an offset past the last line with a sentence; `edit` reports what it changed with one; `skill` writes two of the guidance lines around the body differently, the relative-paths line (`crates/vibe-core/src/tools/builtins/skill.rs:237`) and the sampling note (`:240`), and writes its own reuse sentence when a skill was already loaded this conversation; `exit_plan_mode` writes a message for each of the six plan-review outcomes; `web_fetch` writes the marker that reports a truncated body, cut at the same bound as the reference; and `task` writes the unknown-agent and not-a-subagent refusals. Every other field of every one of those results still compares byte for byte. The refusals differ from the rest in how they are held: `skill`'s not-found error and `task`'s two are raised, and the replay compares a raised error by kind and by presence rather than by text, so they carry no ledger entry. The corpus records each of them as a presence flag and a SHA-256 of the reference's sentence, so a re-pin that reworded one is visible in its diff | 51 entries pointing at `LICENSING`, over 18 cases, in `LEDGER` in `crates/vibe-app-server/src/tool_execution_parity_tests.rs`, each scoped to one pointer on one case. `a_ledger_entry_whose_divergence_is_fixed_fails_the_suite` deletes an entry the moment a wording converges, and `every_recorded_divergence_names_a_scorecard_row` fails if this row outlives them |
| The skill catalog an unknown name is answered with | The reference answers an unknown skill name with every available name, uncapped and comma-separated, and since v2.25.5 only the model-invocable ones (`vibe/core/tools/builtins/skill.py:177-187`). This port lists at most 40 and then appends what the catalog could not load, naming each unreadable file and its reason. The cap is kept rather than removed because a workspace holding hundreds of skills turns the one surface a mid-turn tool call reads into a page of names either way, and the issue clause is kept because a skill absent for a parse error is otherwise indistinguishable from one that was never written. Both are bounded to the not-found error and change nothing a successful load returns | `MAX_LISTED_SKILLS` at `crates/vibe-core/src/tools/builtins.rs:59` and the unloadable clause at `crates/vibe-core/src/tools/builtins/skill.rs:78-100`; the corpus cases `skill/unknown-name` and `skill/unknown-name-with-none-available` record the reference's refusal as a presence flag and a SHA-256, which the replay compares by kind and by presence rather than by text, so this row carries no ledger entry and is held by `every_accepted_divergence_still_names_evidence_that_exists` in `crates/vibe-core/src/parity/ledger_tests.rs`, which fails the moment the cap stops existing |
| The checkpoint log holds at most 512 MiB | The reference constructs its log unbounded (`vibe/core/checkpoints/checkpointer.py:56`), empties it when its loop is rebound to another session (`vibe/core/agent_loop/_loop.py:3547-3558`), and since v2.24.1 keeps it across a compaction. This port holds one log per attached session inside a server process that outlives any single conversation, so an unbounded log is an unbounded process. The ceiling refuses further capture rather than trimming: a region identity a client already holds must never stop resolving because the log made room behind it. Reaching it publishes one warning on `diagnostics/list` and leaves the file writes themselves untouched | `RETAINED_BYTES_LIMIT` in `crates/vibe-core/src/checkpoints/checkpointer.rs`, asserted by `the_log_is_never_shortened_behind_the_caller` and `a_full_log_refuses_the_capture_and_publishes_it` |
| `edit` decodes a narrower codec set | The reference tries the byte-order mark, UTF-8, the locale codec and then `charset-normalizer`, a statistical detector. This port recognizes the mark, UTF-8, and falls back to Latin-1, which decodes every byte, so no file is refused and every one of the three round-trips byte for byte. A file in a multi-byte codec with no mark decodes as Latin-1 and is written back unchanged unless the edit touches it | `crates/vibe-core/src/workspace/text_file.rs` and its round-trip tests |
| A telemetry endpoint is HTTPS only | The reference derives the datalake endpoint from the active Mistral provider's `api_base` with a regex that admits `http://`, and posts a bearer token to whatever it resolves. This port refuses a resolved endpoint that is not HTTPS, or that carries a credential of its own, and answers no target instead, which silences telemetry rather than sending the key in the clear. Nothing the reference reaches over HTTPS becomes unreachable; the corpus's eight documents all resolve, and the `envelope` family conforms on every one | `telemetry_endpoint` in `crates/vibe-core/src/telemetry.rs`, asserted by `the_endpoint_is_derived_from_the_provider_base` |
| The crash reporter is absent | The reference ships Sentry dormant at the pinned commit: `_CLI_SENTRY_DSN` and `_ACP_SENTRY_DSN` are both `None` (`vibe/observability/sentry.py:15-16`), and `init_sentry` answers false through its own `is_initialized` check (177-209) unless the `SENTRY_DSN` environment variable is set, which `sentry-sdk` reads when the DSN it is given is `None` (`sentry_sdk/client.py:319-320` in the pinned environment, the same 2.64.0 release at both pins). The earlier statement that it never initializes was too strong. Adding `sentry-rust` would create a capability the reference does not have, which is the same reasoning as the dormant remote registry row below; this port initializes no crash reporter at all, and what a crash leaves behind is the log file at `$VIBE_HOME/logs/vibe.log`, which the same PRD shipped. Revisit only if the reference ever sets a DSN, which is measured rather than assumed: the corpus records both DSNs as configured-or-not and the replay compares each against false | `constants/sentry` in `crates/vibe-core/src/telemetry/telemetry_parity_tests.rs`, captured by `scripts/parity/telemetry.py` and asserted by `the_committed_corpus_replays_against_this_port`, which fails the moment either DSN stops being null, as does the recapture probe |
| `user_plan` is carried and never filled | The reference fills the census field from the console's `whoami` answer, mapping its plan type and plan name onto four vocabularies (`vibe/setup/auth/whoami.py:286-330`, applied to the loop at `vibe/core/agent_loop/_loop.py:1232-1268`, and since v2.25.x also derived at session start by the experiments initialization). This port classifies `ready`, `missing_key` and `disabled` locally and has no client for that endpoint, which is the `account/read` row below; none of the three carries a plan name, so the field is absent rather than fabricated from a status that does not name a plan. The census declares it and it travels the moment a console client exists | `TelemetryBaseMetadata::user_plan` in `crates/vibe-core/src/telemetry.rs`, produced from `TelemetryContext::user_plan`, and the `account/read` row below, which records the missing console client |
| `vibe.admin_config_applied` has no producer | The reference raises it where an org-managed configuration layer is applied (`vibe/app_server/_admin_config.py:35-52`, reached from `vibe/app_server/_resources.py:662-696`), reporting the outcome, the number of enforced keys and whether an error occurred. This port composes no such layer: its seven configuration layers are Defaults, Discovered, SelectedToml, Experiments, Environment, Runtime and Agent, so there is no application for the event to report on. The name is left undeclared rather than shipped without a producer, which is what keeps the vocabulary honest, and it arrives with the layer if the layer ever does | `TelemetryEvent` in `crates/vibe-core/src/telemetry.rs` publishes 25 of the 27 names the capture reads, and the four `admin_config_applied` entries of the ledger in `crates/vibe-core/src/telemetry/telemetry_parity_tests.rs` fail the replay the moment the gap closes |
| The compaction envelope's two prose runs | `NOTICE` forbids shipping upstream prose, and the envelope carries two runs of it: the sentences that open it and the one that introduces the summary. This port writes its own covering the same three directives, so the envelope reads the same and no reference sentence ships. Everything its readers depend on is held to the reference byte for byte: the four reserved tags, the line layout, one element per preserved message, the escaping and the summary block. The corpus records each prose run's length and SHA-256 so a change on either side is still detected | `envelopeProse` in `crates/vibe-core/src/compaction/compaction_parity_tests.rs`, whose two ledger entries fail the replay if either run ever conforms, against `envelopeRenders` 8/8 conforming on structure |
| Builtin skill prose | The reference seeds two builtin skills, `vibe` and `skill-creator`, whose descriptions and bodies total 65 102 characters of authored prose at the pin (40 589 bytes at `b78b451`) that `NOTICE` forbids shipping. This port seeds the same two names with the same structure, invocability split, `builtin` source and pathless projection, and writes both descriptions and both bodies originally against the same directive coverage: the `vibe` body documents the home, configuration, models, agents, skills, tools, commands, flags, hooks, MCP, connectors, trust, mentions, logs, themes and voice of this port, with its documentation link pinned to the running `[workspace.package]` version, and the `skill-creator` body covers one-question-at-a-time gathering, the `SKILL.md` shape, the five discovery roots, the reserved names, support files and the permission flow. The directive coverage is no longer complete: since v2.24.1 the reference `vibe` body also covers OpenTelemetry tracing, session titles and plugins, and the `skill-creator` body covers `disable-model-invocation`, none of which this port's bodies mention yet. The prose inequality is permanent; that coverage gap is open. The corpus records each reference prose run as length plus SHA-256, so structure is compared for equality and prose for permanent inequality | The four `builtins/builtinProse-*` entries in `DIVERGENCES` in `crates/vibe-core/src/skills/skills_parity_tests.rs`, plus `command/builtin-skill-creator` for the slash invocation that delivers the `skill-creator` body, whose stale check fails the replay the moment either body or description conforms to the reference digest, so the divergence can never be closed by copying |
| The registry store's fallback description | A materialized registry skill whose payload carries no description is published under a generated sentence, and the reference's sentence is authored prose `NOTICE` forbids shipping. This port writes its own naming the skill and its registry provenance; the reference sentence was reworded at v2.25.x and no longer names the registry, and the corpus records the reference's only as length plus SHA-256, masked as `{fallbackDescription}` in every recorded tree, so the store comparison stays structural while the prose stays original | `fallback_description` in `crates/vibe-core/src/skills/registry/store.rs` and the `store/fallbackDescription` entry in `DIVERGENCES` in `crates/vibe-core/src/skills/skills_parity_tests.rs`, whose stale check fails the replay the moment the sentence conforms to the reference digest |
| The Windows terminal ladder is one rung | Reference `_windows.py` tries ConPTY and then WinPTY, keeping each failure and raising once both are exhausted. `portable-pty` publishes only the ConPTY implementation, and adding WinPTY would mean a new workspace dependency no story here takes on, so the ladder this port walks is one rung long on Windows and one on POSIX. Nothing degrades quietly behind it: the names and the order are the reference's, a host where the rung does not start fails the session instead of falling back to pipes, and the failure names every rung it tried | `PTY_BACKENDS` in `crates/vibe-core/src/pty.rs`, asserted by `the_missing_rung_is_the_one_the_scorecard_records`, which fails the moment a second rung is added without restating this row, and by `a_session_whose_terminal_cannot_start_fails_instead_of_running_on_pipes` in `crates/vibe-core/src/tools/shell/tests.rs` |
| The placeholder a failed summarization falls back to | Outside strict mode the reference still compacts when neither summarization call produced a usable summary, and the summary it writes into the envelope is an authored sentence `NOTICE` forbids shipping. This port writes its own, so the conversation degrades the same way and no reference sentence ships; the decision tree around it, both failure reasons, the fallback and the retry ladder, is held to the reference exactly | `placeholderSummary` in `crates/vibe-core/src/compaction/compaction_parity_tests.rs`, whose ledger entry fails the replay if the wording ever conforms |
| The compaction request carries no telemetry call type | The reference labels its summarization request `secondary_call`, the call type its telemetry client reads back into the request census. Every model call this port makes now reports `vibe.request_sent`, and each one reports `main_call`: the summarization request is made through the same path as the turn's own, so it is marked through the provider metadata instead of a census of its own. The request itself, its transcript copy, its tools and its tool choice are the reference's | `managerCallType` in `crates/vibe-core/src/compaction/compaction_parity_tests.rs`, whose ledger entry fails the replay when a call type starts conforming, against `TelemetryCallType` in `crates/vibe-core/src/telemetry.rs` |
| The context warning's message is original prose | `NOTICE` forbids shipping the reference's warning sentence, so this port writes its own naming the same three quantities: the share of the window consumed, the current token count and the window. Only the `vibe_warning` tag is reproduced verbatim, because it is an identifier a client matches on rather than prose | `ContextWarningMiddleware` in `crates/vibe-core/src/middleware.rs`, asserted by `the_context_warning_fires_once_at_half_the_window` |
| The context-warning latch clears on the compaction reset alone | The reference clears the latch on either reset reason, and resets its pipeline only when the conversation is compacted or cleared. This port also resets at the top of every turn, so clearing on both reasons would warn once per turn instead of once per session. Clearing on the compaction reason alone is what reproduces the reference's observable cadence under this port's own | `ContextWarningMiddleware::reset` in `crates/vibe-core/src/middleware.rs`, asserted by `the_context_warning_is_silent_without_a_window_and_relatches_on_a_compaction` and by `the_context_warning_reaches_the_model_once_per_session` in `crates/vibe-app-server/src/client.rs` |
| `identity/read` is declared and unrouted | The name arrived upstream at v2.24.0 and the `b78b451` re-pin brought it into the inventory, so the earlier statement that it exists nowhere in the reference tree is false at this pin and was removed. It is declared in `SERVER_METHODS`, so a client reading the inventory sees the same name, and routing it is app-server parity work rather than compaction work. `workspace/worktrees/list` arrived at the same pin and left this row when US-281 routed it; v2.25.x then retired that name for `workspace/git/worktrees/list`, which the census now records as a retired method this port still serves | `UNROUTED_METHODS` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs`, which fails the replay when it becomes routed |
| `ConfigView` omits the three unpinned-model fields | v2.24.0 added `activeModelPinned`, `defaultModelAlias` and `showGreeting`, all three produced from an `active_model` that may be left unpinned and resolved on read. Producing them means porting that feature, which is configuration parity work. The v2.25.7 re-pin widened the gap: the view now also lacks `logLevel`, `showSubagentStatusList`, `worktreeLimit` and `experimentalEnableTabStatus`, still publishes the `vibeCodeEnabled` upstream removed, and `ModelConfigView.displayName` is absent. Until then `config/read`, `runtime/read`, and since the re-pin `session/list` and `session/read`, are recorded as diverging from the census rather than validated against a shape this port cannot fill | `DIVERGENT_RESPONSES` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs`, which fails the replay when either response starts validating |
| `RuntimeMutationStatus` and `SessionKind` are unmodeled | The v2.25.7 re-pin added both vocabularies: the status field `RuntimeMutationResponse` gained, and a session lifecycle role. Nothing in this port produces either, so the names are recorded rather than answered with a vocabulary no code produces. `PublicRetryCategory` left this row when the backend retry loop started classifying retries and `turn/retrying` started publishing them | `UNMODELED_ENUMS` in `crates/vibe-app-server/src/app_server_surface_parity_tests.rs` |
| The model call failure sentences are original prose | A failed model call carries two authored sentences upstream: the public message `turn/error` publishes, and the provider message a backend error reports when the provider sent none. `NOTICE` forbids shipping either, so this port writes its own naming the same cause, status and next action. The corpus records each reference sentence as a length plus a SHA-256, after replacing the stand-in address, the closed port and the package version with placeholders, so codes, details, statuses and classification compare for equality and the prose for permanent inequality: a sentence that ever matches a reference digest fails the replay | `digest` and the prose comparison in `crates/vibe-core/src/llm/backend_parity_tests.rs`, and `provider_message` in `crates/vibe-core/src/llm/error.rs` |
| A model endpoint is HTTPS unless it is a loopback host | The reference posts the credential to whatever `api_base` names, plain HTTP and userinfo included. This port refuses, when the backend is built, an address that carries credentials or uses plain HTTP to a host that is not loopback, which is the same rule its telemetry endpoint follows. Every oracle scenario runs on loopback, so the replay never meets the guard; a `vertex-anthropic` provider is addressed by its region and is exempt | `check_address` in `crates/vibe-core/src/llm/completion.rs`, built on `is_secure_transport` in `crates/vibe-core/src/text.rs` |
| Two Mistral client timeouts are declared and not applied | The reference hands `api_write_timeout` and `api_pool_timeout` to the httpx client under the Mistral SDK, next to `api_connect_timeout`. This port declares and reads all three and applies the connection bound, but its HTTP client exposes neither a write deadline nor a pool acquisition deadline, so a stalled upload or an exhausted pool waits on the request timeout and the retry budget instead. No oracle scenario reaches either path, and `GenericBackend` applies none of the three upstream | `connect_timeout` in `MistralBackend::new` in `crates/vibe-core/src/llm/mistral.rs`, and `ApiSettings` in `crates/vibe-core/src/provider/config.rs` |
| Workload identity federation credentials are refused | Upstream `google.auth.default` also accepts an `external_account` credentials file, which exchanges a third-party token through Security Token Service. This port reads the user refresh token, the service account key, the impersonated service account and the metadata server, and refuses an `external_account` file with a configuration failure rather than ship a token exchange nothing here exercises | `from_info` in `crates/vibe-core/src/llm/vertex.rs`, asserted by `a_credentials_file_is_read_by_its_type` |
| The eleven sign-in error sentences | The reference names each browser sign-in failure in an authored sentence `NOTICE` forbids shipping. This port produces the same eleven codes in the same declaration order from the same conditions, and writes its own sentence for each, naming the same cause and the same next action. The corpus records every reference sentence as a length plus a SHA-256, so codes and order are compared for equality while the prose is compared for permanent inequality: a sentence here that ever matches a reference digest fails the replay, so the divergence can never be closed by copying | The `errorTaxonomy` prose comparison in `crates/vibe-core/src/setup_auth_parity_tests.rs`, running 23/23 conforming on the codes, their order and the inequality of all eleven sentences |
| The onboarding screen text | Every heading, subtitle, option label, hint and status line the reference's seven onboarding screens draw is authored prose, and the same is true of the sentences its exit paths print. This port writes its own for each, covering the same directives, and the corpus records the reference's runs per module as a length plus a SHA-256 and never as text. The replay digests every sentence this port's onboarding modules carry and fails if one ever matches. Three runs are reproduced on purpose and listed with their reason, each a name rather than prose: the product name, the organization name, and the greeting fragment that introduces the product | `screenProse` and `REPRODUCED_NAMES` in `crates/vibe-cli/src/tui/onboarding_parity_tests.rs`, at 60/60 conforming, plus the `terminating` message digests in the same module; each reproduced name is stale-checked and fails once it stops matching |
| The ACP terminal sign-in relaunches this executable | The reference announces `sys.executable` for its terminal sign-in method, and when that is a Python interpreter it passes its own script path before `--setup`, so the capture records `python3` and the `vibe-acp` script as the first argument. A native executable has no interpreter to name: this port announces itself with `--setup` alone, which is the reference's own branch for an executable that is not Python, and `vibe-acp --setup` hands the terminal to `vibe --setup` | `terminal_method` in `crates/vibe-acp/src/auth.rs`, and the `initialize/terminal-auth` entry of `LEDGER` in `crates/vibe-acp/tests/acp_parity_tests.rs`, which fails the day the difference stops reproducing |
| The ACP authentication method labels | The reference names and describes each editor-protocol authentication method, and labels the terminal one, in authored prose an editor renders verbatim. This port advertises the same method ids under the same client-capability gates and writes its own name, description and label for each. The `acpAuthProse` family records the five reference runs as length plus SHA-256 only, and the guard asserts both directions: every published label differs from the reference's, and every label is still a real sentence rather than an empty string that would differ for the wrong reason | `crates/vibe-acp/src/auth/prose_tests.rs`, reading the `acpAuthProse` family of `crates/vibe-core/tests/setup-auth/corpus.json`, whose surface list must match what the crate publishes so no label goes unchecked |
| The sign-in poll interval is 3.0 seconds with no backoff | RFC 8628 §3.5 recommends a 5 second poll interval with a `slow_down` escalation, and the reference polls at 3.0 seconds with neither. Parity wins over the recommendation deliberately: a different interval issues a different number of requests over the same window, which is a divergence the corpus measures directly, and the loop is already bounded by the server's recorded expiry. The deviation is recorded here rather than silently inherited | `POLL_INTERVAL_SECONDS` in `crates/vibe-core/src/auth/sign_in.rs`, asserted by `constants/pollIntervalSeconds` and by the per-scenario `sleeps` of `signInProtocol` in `crates/vibe-core/src/setup_auth_parity_tests.rs`, either of which fails if the cadence changes or a backoff appears |
| Onboarding is measured as observations, not as rendered cells | Textual and ratatui cannot produce the same cells, so no story asserts terminal output against the reference's snapshots. The capture drives the real reference app through Textual's headless pilot and records the installed screen set, the transitions taken, the focus target per screen, the validation class per input state, the effects persisted and the terminating value; no cell, SVG or styled text enters the corpus. The precedent is the chat-input harness, which established the same measurement for the composer | `scripts/parity/onboarding.py` and `crates/vibe-cli/src/tui/onboarding_parity_tests.rs`, whose corpus carries observations only and whose screen-graph family compares 339 fields per replay at the pin |
| The update prompt installs by rerunning the installer | The reference offers an update from inside its dialog, runs the package-manager upgrade commands it was installed through, and reports one of four outcomes. This port now publishes the same four outcomes and the same exit codes, and runs its own upgrade commands: the shell and PowerShell installers this repository ships, because no package manager publishes this binary. The dialog, its choices, its labels and its two modes are the reference's; the divergence is which command runs and the manual sentence the failed branch prints, which is authored prose `NOTICE` forbids reproducing | `the_upgrade_commands_rerun_an_installer_this_repository_publishes` in `crates/vibe-cli/src/distribution/release_parity_tests.rs`, which fails the moment the list stops being one installer rerun resolved from `[package] repository` and becomes a package-manager upgrade the way `UPDATE_COMMANDS` is; the list itself is `upgrade_commands` in `crates/vibe-cli/src/distribution.rs`, `UpdatePromptResult` and `update_failed_message` are in `crates/vibe-cli/src/tui/updates.rs` and `run_upgrade_commands` in `crates/vibe-core/src/updates.rs`; the message is replayed as a declared divergence against the committed trace by `crates/vibe-cli/src/tui/runtime_parity_tests/terminal_services.rs` |
| `account/read` cannot answer `unauthorized` | The reference reaches `GET {console_base_url}/api/vibe/whoami` with the bearer credential and maps the answer onto its plan vocabulary; the client moved to `vibe/setup/auth/whoami.py` at v2.24.2 and now also caches its answer for six hours. This port classifies three of the four statuses locally and has no client for that endpoint, so a key that is present but revoked reads as `ready` here and `/whoami` shows a null plan. Closing it needs a console client with its own credential handling, which is app-server parity work rather than setup work, and the setup parity work listed it as a non-goal for that reason | The documented classification in `crates/vibe-app-server/src/workspace.rs`, which reaches `ready`, `missing_key` and `disabled` and never claims the console's verdict |
| The standalone `vibe-app-server` command is absent | The reference declares three console scripts in `[project.scripts]` and ships `vibe-app-server.spec`, but its own build workflow runs `build-pyinstaller-binaries.sh vibe-acp.spec vibe.spec` and never builds the third (`.github/workflows/build-and-upload.yml` at the pin), so the only channel that publishes `vibe-app-server` is the PyPI install `[project.scripts]` feeds, which this port does not use. The app server is not absent here, only its standalone stdio command: `vibe` constructs an `AppServer` in process and `vibe-acp` links the same crate, so both published binaries serve the same session methods. Since v2.25.7 the `vibe` script targets `vibe.cli.launcher:main`, and the Rust client it launches under `VIBE_CLI=rust` spawns `vibe-app-server`, but that path ships in the wheel and not in the PyInstaller build. Adding a third binary would publish an entry point the reference's own release path does not carry | `PUBLISHED_BINARIES` and `the_release_publishes_exactly_the_two_binaries_the_ledger_records` in `crates/vibe-cli/src/distribution/release_parity_tests.rs`, which fails on a third packaged binary, on a third publishable `[[bin]]` in the workspace, and on either installer naming `vibe-app-server` |
| The release notes are compiled into the binary | The reference resolves `VIBE_ROOT / "whats_new.md"` at run time and answers nothing when the file is missing (`vibe/cli/update_notifier/whats_new.py:30-37`, with a `VIBE_WHATS_NEW_FILE` override its own comment marks as test-only), which is what a PyInstaller onedir layout needs. This port embeds the file with `include_str!`, so the notes ship inside the executable and an archive that drops the file still shows them. The observable contract is the stronger one: the reference can degrade to no notes and this port cannot, and nothing upstream depends on replacing the file beside the binary | `whats_new_content` in `crates/vibe-cli/src/tui/updates.rs`. Its `include_str!` fails the build if the file stops shipping, and its `Option<&'static str>` return is what `release_notes_are_present_and_bounded` binds, so closing the divergence by reading a runtime root cannot compile against that signature and has to move this row with it; the heading is held to `[workspace.package] version` by `the_release_notes_heading_names_the_workspace_version` in `crates/vibe-cli/src/distribution/release_parity_tests.rs` |
| Production resolves the GitHub gateway where the reference resolves PyPI | The reference implements both adapters and constructs only one: `PyPIUpdateGateway(project_name="mistral-vibe")` at `vibe/cli/cli.py:385` and `vibe/cli/textual_ui/app.py:6567`, while `GitHubUpdateGateway` is exported from `vibe/cli/update_notifier/__init__.py` and constructed nowhere outside its own tests. This port publishes GitHub release archives and no PyPI package, so resolving the reference's choice would compare a user's binary against a different product's index and advertise a command that installs that product. Both adapters are ported and both stay measured; the divergence is the resolution, not the surface | `production_update_gateway` in `crates/vibe-cli/src/tui/startup/update.rs`, held to the repository `[package] repository` declares by `the_update_gateway_names_the_repository_the_manifest_declares` in `crates/vibe-cli/src/tui/startup/update/gateway_tests.rs`; the retained `PyPiUpdateGateway` stays measured by the 13-event `update-gateway` trace in `crates/vibe-cli/tests/runtime-parity/terminal-services.json`, which fails if the adapter is removed rather than merely unresolved |
| The reference raises on a malformed update payload or cache | Six inputs send the reference's update notifier into an exception nothing above it catches: a truthy `versions` that is not iterable (`TypeError`), a releases payload that is a JSON object or a release whose `tag_name` is not a string (`AttributeError`), and a legacy `update_cache.json` holding a list, a string or a number (`AttributeError` out of `get`). Upstream the forced check then ends in a traceback and the background check logs and gives up. This port answers each with the absence its caller already handles, no update or no cache, because a crash is not a behavior a user can build on | The `divergences` of `update-gateway` (events 20, 28 and 29) and `update-cache-faults` (events 0 to 2) in `crates/vibe-cli/tests/runtime-parity/terminal-services.json`, replayed by `corpus_replays_update_attention_narration_and_exit_behavior` in `crates/vibe-cli/src/tui/runtime_parity_tests/terminal_services.rs`, which fails on a declared divergence that stops reproducing |
| The update gateways identify this port | The reference's GitHub gateway sends `User-Agent: mistral-vibe-update-notifier` and its PyPI gateway the `httpx` default. This port sends `mistral-vibe-rs-update-notifier/<version>` to GitHub, which answers an unidentified client with a 403, and nothing of its own to PyPI. Sending the reference's name would attribute this binary's traffic to upstream in both indexes' logs; the request path, the `Accept` header, the token and every branch on the answer are the reference's | `GITHUB_USER_AGENT` and `exchange` in `crates/vibe-core/src/updates.rs`, whose request shape `the_github_gateway_asks_for_the_repository_releases_and_selects_one` asserts |
| Linux binaries load `libasound.so.2` with the process | The reference records audio through miniaudio, which opens ALSA at run time, so it starts on a host without it. This port records through `cpal`, whose `alsa-sys` links `libasound` dynamically through pkg-config. Opening it at run time instead would need `unsafe`, which the workspace forbids, and linking a static `libasound.a` breaks the plugin chain: the PipeWire and PulseAudio plugins ALSA loads link the system `libasound.so.2`, which would put two copies of the library in one process. `libasound2` (Debian, Ubuntu) or `alsa-lib` (Fedora, RHEL) is therefore a runtime requirement of `vibe` on Linux, and the only shared library outside the C runtime either release binary may link. `vibe-acp` links none | The library allowlist of `scripts/ci/check-linux-portability.sh`, run on both binaries by the Linux legs of `.github/workflows/release.yml` and held there by `every_linux_leg_builds_on_the_reference_glibc_floor_and_proves_it` in `crates/vibe-cli/src/distribution/release_parity_tests.rs` |
| The distribution is two native executables, not a Python package | The reference ships a wheel assembled by `build_backend/maturin_backend.py` around its own Rust harness and `vibe-rs` client, chooses between its Python and Rust clients through `VIBE_CLI` in `vibe/cli/launcher.py`, and freezes onedir PyInstaller bundles from `vibe.spec` and `vibe-acp.spec` with a runtime hook that routes TLS through the operating-system store (`pyinstaller/runtime_hook_truststore.py`). This port is itself a native client: there is no second implementation for `VIBE_CLI` to select, no wheel for a build backend to assemble and no interpreter to freeze, so none of those files has a counterpart. What the frozen layout gives a user is reproduced by other means: `vibe` and `vibe-acp` start without extraction and carry their release notes, TLS verifies against the operating-system store under either value of `enable_system_trust_store`, as the hook makes the frozen reference do, and `SSL_CERT_FILE` and `SSL_CERT_DIR` add to that store as `build_ssl_context` adds them. The Linux binaries hold the manylinux_2_28 floor the reference builds on | `crates/vibe-core/src/http_trust.rs` with `http_trust_tests`, `PUBLISHED_BINARIES` in `crates/vibe-cli/src/distribution/release_parity_tests.rs`, and `scripts/ci/check-linux-portability.sh` |
| Release archives and installers are this repository's own | The reference publishes one archive per binary, `vibe-{os}-{arch}-{version}.zip` and `vibe-acp-{os}-{arch}-{version}` in zip and tar.gz, with `darwin` for macOS, and its `scripts/install.sh` installs `uv` and runs `uv tool install mistral-vibe`. This port publishes one archive per target, `mistral-vibe-rs-{version}-{target}`, carrying both binaries, the completions, `LICENSE` and `NOTICE`, plus an aggregate `SHA256SUMS`, and its installers download and verify those. The archives' only consumers are this repository's installers and action, which resolve these names; reusing upstream names would let a URL rewrite install a different product without a failing checksum | `the_release_workflow_publishes_every_target_an_installer_can_ask_for` and `both_installers_fetch_from_the_declared_repository` in `crates/vibe-cli/src/distribution/release_parity_tests.rs` |
| The composite action installs the release binary | The reference action runs `vibe -p` from its own source checkout after installing `uv`, and installs Python only when `python_version` is set, because its default-version step compares a string with a boolean and never runs. This port declares the same four inputs with the same requirements and defaults, installs the requested Python under the same condition and runs `vibe -p` from the workspace, but installs the signed release binary instead of `uv` and a source tree. Its three further inputs, `version`, `release_base_url` and `approvals` (default `ask`), are optional and its `result` output is additive, so a reference workflow never sets them and behaves as upstream | `the_action_declares_every_reference_input_unchanged_and_adds_only_optional_ones` and `the_recorded_reference_inputs_match_the_pinned_checkout` in `crates/vibe-cli/src/distribution/action_parity_tests.rs` |
| Experiments and GrowthBook | Requires access to a third-party Mistral service with credentials this repository does not hold. The `experiments` table stays as the injection point | The `experiments` field of `crates/vibe-core/src/config/registry.rs`, declared and published with no consumer |
| VS Code extension promo | Advertises an extension that does not target this binary | No promo surface exists in `crates/`, and the parity table scores the part 10 for that reason |
| Configuration fingerprint format | Upstream builds the token from `st_dev:st_ino:st_mtime_ns:st_size` (`vibe/core/config/fingerprint.py:32-35`); this port digests the file contents. The token is opaque, is only ever compared against one produced by the same implementation, and a content digest detects an edit that restores the size and timestamp | `crates/vibe-core/src/config.rs`, `fingerprint_optional` and the concurrent-edit tests |
| Per-layer async state machine | Upstream caches each layer, forces reloads and resolves trust once per layer (`vibe/core/config/layer.py:206-228`; the per-layer trust transitions were removed at v2.25.x); this port recomposes from disk on every `load()`, which is observably equivalent for every current caller | `load` in `crates/vibe-core/src/config.rs`, which recomposes the selected layers from disk on every call |
| The completion index answers in relative-path order | The reference holds a rebuilt index in `scandir` order and sorts only after an incremental update invalidated its cached order (`vibe/cli/autocompletion/file_indexer/store.py:80-114`); since v2.25.3 a root inside a git work tree is listed through `git ls-files` and returned sorted by relative path, so the enumeration order survives only on the non-git walk, so what a rebuild returns is whatever the filesystem enumerated and is not a contract anything can conform to. This port holds one map keyed by relative path, which is that order permanently. Both sides are compared under the same normalization the `grep` row above already records: the capture sorts the reference's snapshot by relative path before recording it, and the replay reads a map already in that order, so the entry sets are compared and the enumeration order is not | `WorkspaceIndex` in `crates/vibe-cli/src/tui/completion/path.rs`, whose `entries` is a `BTreeMap` keyed by the relative path, and `_entries` in `scripts/parity/autocompletion.py`, which sorts the reference snapshot before it is recorded |
| The index rebuild has no background executor | The reference rebuilds on a thread-pool executor, cancels the task belonging to a root before starting another and tracks the root in flight (`vibe/cli/autocompletion/file_indexer/indexer.py:125-182`). This port rebuilds synchronously inside the completion reader, which is already off the terminal event path, so the executor, its per-root cancellation and its `_target_root` bookkeeping have no observable consequence for any current caller: the same entries, the same counters and the same ranking answer the same query. Same reasoning as the per-layer async state machine row above | `WorkspaceIndex::rebuild` in `crates/vibe-cli/src/tui/completion/path.rs`, which walks the root and replaces the map in one call |
| `WALK_SKIP_DIR_NAMES` has no counterpart | The reference exports the constant from `vibe/cli/autocompletion/file_indexer/ignore_rules.py:84-89` and imports it nowhere at the pinned commit, so reproducing it would ship a constant with no consumer and no behavior to measure. The absence is recorded by the autocompletion oracle as a ledgered divergence rather than as a match, so a reference that starts using it, or a build that starts declaring it, fails the replay instead of passing quietly | `constants/walkSkipDirNames` in the `DIVERGENCES` ledger of `crates/vibe-cli/src/tui/completion/path/autocompletion_parity_tests.rs`, whose staleness check fails the suite once the case conforms |
| Playback degrades to a reported absent capability | The reference probes its output backend and reports an absent one, now through miniaudio rather than sounddevice since v2.25.5 (`vibe/cli/audio_player/audio_player.py:31-45`), and raises when playback starts without one. Neither a host without a backend nor one without an output device can be stood up in CI, so this port reports the absent capability instead of claiming coverage: the cause is carried as a typed variant rather than as message text, the narrated turn stays successful, and the operator is told once per session rather than once per turn. The absent-backend branch is the only one a test exercises, which is the residual the voice row above records | `PlaybackError::BackendUnavailable` in `crates/vibe-cli/src/tui/voice/player.rs` and `report_speech_failure` in `crates/vibe-cli/src/tui/mod.rs` |
| 5 configuration keys with no upstream counterpart | `thinking`, `notifications`, `proxy`, `tls_ca_path` and `dotenv_path` have no lossless reference target, and mapping them would reinterpret values already on disk | `crates/vibe-core/src/config/surface_parity_tests.rs`, which fails if the set changes |
| An unregistered key survives the merge | The reference merge drops a key its schema does not declare; keeping it lets a file written by a newer client round-trip through this one | `ConfigSnapshot::unregistered_keys`, asserted per corpus scenario |
| `config/batchWrite` | Writes several configuration targets in one request. The reference retired `config/patch` in the v2.25 line for `config/write`, which takes a list of operations and can itself write several targets in one request, so the extension now duplicates a reference method this port does not declare rather than filling a gap. `vibe-cli` settings screens already depend on the atomic form; retiring it is its own migration | `LOCAL_EXTENSION_METHODS` in `crates/vibe-protocol/src/methods.rs`, kept out of `SERVER_METHODS` and out of `ServerCapabilities.methods`, asserted by `app_server_surface_parity_tests` |
| `connectors/toggle` | Enables or disables one connector. The reference now does this through `connector_catalog/toggle`, which this port does not declare, and its ACP agent serves a `_connectors/toggle` extension. Live callers in `vibe-cli` | Same as above |
| `session/overrides/write` | Holds a model, a mode, a thinking level, a reasoning effort and an approval stance for one session's lifetime. Upstream none of the five is session-scoped: the model and the thinking level are configuration writes and the mode and the approval stance come from an agent profile. They used to ride on `session/settings/update`, which made that method accept five fields its reference model forbids; moving them here left it exactly `sessionId`, `maxTurns` and `maxTokens` | Same as above, plus `settings_update_is_strict_and_applies_to_the_next_turn_while_active`, which asserts each of the five is answered with `invalid_params` on the reference method |
| The help document's authored lines | Reference `get_help_text` (`vibe/cli/commands.py:303`) builds three headings, eight shortcut lines and two prefix lines out of authored prose `NOTICE` forbids shipping. This port writes its own thirteen lines covering the same directives, and the corpus records the reference's as a byte length plus a SHA-256 rather than as text. The split is measurable rather than a blanket exemption: the command lines are the registry's own contract, and every one of the thirty-four this port renders under the full context hashes to the reference digest at the same offset. The thirteen authored lines diverge on purpose, in both length and digest, and each one is its own ledger entry; the `/help` message that carries them differs by the same lines and by nothing else the handler emits | The twenty-six `helpProse/*` entries and `handlers/effects/help` in `DIVERGENCES` in `crates/vibe-cli/src/tui/commands_parity_tests.rs`, whose stale check fails the replay the moment one of this port's lines conforms, plus `this_ports_help_prose_never_matches_a_reference_digest` in the same module |
| The quit line carries `Ctrl+D` | The reference lists eight shortcut slots and no `Ctrl+D` among them. This port binds it, next to `Ctrl+C`, so a help that omitted it would hide a working key. It rides on the quit line rather than adding a ninth slot, which keeps the section at the eight lines the corpus recorded while the line states what this binary answers | `SHORTCUTS` in `crates/vibe-cli/src/tui/help.rs`, which is the table `chord_of` routes by, asserted by `the_shortcut_lines_state_what_this_binary_actually_binds` |
| A refused submission is a diagnostic, not a toast | Reference `_warn_not_queueable` (`vibe/cli/textual_ui/app.py:1975`) publishes its five refusals (a slash command or a teleport while a job runs, a shell command while a turn runs, input while a shell command runs, and a side-channel command while another holds the channel) through `App.notify` at `warning` severity, a transient overlay Textual paints over the layout and expires on a timer. This port has no toast surface: its equivalent channel is the diagnostics line, which is where every other refusal this client raises already lands, so a refused line is written there instead. Which reason is given and which remedy is named are measured and conform, the draft still returns to the composer, and nothing is queued; only the surface that shows the sentence differs, and the sentences themselves are original prose because `NOTICE` forbids shipping the reference's | `route` and `refusal_message` in `crates/vibe-cli/src/tui/submission.rs`, whose reason and remedy the `dispatch` family of `crates/vibe-cli/src/tui/commands_parity_tests.rs` compares over six composer states, plus `a_refused_line_is_restored_with_its_reason` in `crates/vibe-cli/src/tui/workflow/command_line_tests.rs` |
| The continuation `/retry` submits | Reference `build_retry_prompt` (`vibe/utils/retry_prompt.py`) writes the instruction `/retry` injects after an interrupted response, with the operator's extra instructions appended. It is a prompt the model reads, which `NOTICE` names first among what may not be shipped, so this port submits its own words covering the same directive, injected the same way and only under the same conditions: no turn running and an offer a retryable failure left. The corpus records the reference's text as a length and a SHA-256, so the two scenarios that submit it differ by the digest alone | `retry_prompt` in `crates/vibe-cli/src/tui/command_handlers.rs`, and `handlers/effects/retry` and `handlers/effects/retry-instructions` in `DIVERGENCES` in `crates/vibe-cli/src/tui/commands_parity_tests.rs` |
| The rewind line states its condition | The reference's eighth slot names the double-escape rewind and states its empty-input condition in a parenthetical (`vibe/cli/commands.py:314`), at this pin and at the previous one; this row used to say it stated none, which was wrong. This port's `escape` handler only rewinds on an empty prompt, so the line says so: a line that advertised an unconditional rewind would be wrong here for every non-empty composer | `SHORTCUTS` in `crates/vibe-cli/src/tui/help.rs` and `escape` in `crates/vibe-cli/src/tui/shortcuts.rs`, asserted by `the_shortcut_lines_state_what_this_binary_actually_binds` |
| No builtin tools directory to override | Reference `_compute_search_paths` (`vibe/core/tools/manager.py:149`) starts its walk at `DEFAULT_TOOL_DIR`, the package directory holding the builtin implementations and their `prompts/*.md` files, so a builtin description is itself read from disk. This port compiles its builtin descriptions into the binary, which leaves that first position without a counterpart: the compiled text is what an operator's file overrides. Everything after it is reproduced position by position | `the_reference_reads_its_builtin_descriptions_from_disk` and `the_search_path_order_matches_the_reference` in `crates/vibe-core/src/tools/descriptions/descriptions_parity_tests.rs`, which compare the reference order with its first entry removed and fail if this port ever resolves one |
| A `tool_paths` entry describes but does not implement | The reference walks each search path twice. `_iter_tool_classes` (`vibe/core/tools/manager.py:168`, the glob in `_iter_tool_classes_with_origin` at `:180`) globs `<dir>/*.py`, and `_load_tools_from_file` (`:196`) executes each file through `importlib` and registers every concrete non-abstract `BaseTool` subclass it finds in the module. `_iter_tool_descriptions` (`:238`) reads `<dir>/prompts/*.md` as descriptions. This port reads the second walk only, and the split is deliberate: loading a tool implementation means executing operator Python inside the session, which this binary has no interpreter for and no sandbox around, while a description file is inert text. That makes the divergence permanent rather than a backlog item. `discover_tool_defaults` (`:272`) drives the same class walk to read each class's configuration defaults, so upstream an operator directory contributes per-tool defaults as well as implementations; here the defaults corpus answers for the compiled builtins only. The configuration key, its precedence and its resolution rules are ported in full, so a `tool_paths` entry is honored for descriptions and ignored for implementations. The `ToolSource` enum carried a `Custom` variant for the implementations that walk would have produced, and this epic removed it rather than leaving a reserved name with no producer: the enum now names the three sources that publish a tool here, `BuiltIn`, `Mcp` and `Connector` | `crates/vibe-core/src/tools/descriptions.rs` and its parity replay, which measure the search path order and the winning description file against the reference and assert nothing about class loading, plus `ToolSource` in `crates/vibe-core/src/tools.rs`, which no longer declares a source nothing constructs |
| An MCP disable entry may name a tool by this port's own spelling | Reference `ToolManager._is_source_disabled` (`vibe/core/tools/manager.py:491-505`) disables a server's tool when its `disabled_tools` holds the tool's remote name, and nothing else. This port reads the remote name first, so every entry the reference honors disables the same tool here, a remote name winning over a published name it collides with, and its catalog now writes the remote name as the reference does (measured by the catalog oracle's toggle scenarios). Only an entry naming no remote tool of the server, which the reference ignores, is then read as one of this port's own spellings: the published name earlier builds of this port persisted from their MCP toggle, and the `mcp_{alias}_{tool}` name an older build persisted. Reading the remote name alone would re-enable every tool those builds disabled | `normalize_disabled_tools` in `crates/vibe-core/src/mcp/registry.rs`, pinned by `persisted_disable_entries_resolve_onto_the_published_name` and `a_remote_name_outranks_the_published_name_it_collides_with` beside it; the spelling the catalog writes is `persist_mcp_toggle` in `crates/vibe-core/src/config/integrations.rs` |
| A remote tool name a provider would refuse is sanitized | The reference publishes an MCP tool under the name its server spelled, spaces included, and the provider refuses the request that carries it. This port rewrites such a name into one the provider accepts before publishing it, so the tool stays callable; every name a provider accepts is published as the reference publishes it | The `*/spaced-name` entry of `LEDGER` in `crates/vibe-app-server/tests/mcp_transport_parity_tests.rs`, which fails once it stops reproducing |
| OAuth requests carry reqwest's `Accept` and no `User-Agent` | The MCP SDK builds its metadata, registration and token requests as bare `httpx.Request`s with no `Accept`, and the requests httpx builds itself name the Python library in `User-Agent`. reqwest adds `Accept: */*` to every request and offers no way to remove it, and naming the Python library from this binary would be false. No authorization server reads either header to decide an answer | The `/wire/*/headers/accept` and `/wire/*/headers/user-agent` entries of `LEDGER` in `crates/vibe-app-server/tests/mcp_oauth_parity_tests.rs` |
| The OAuth callback pages are this port's own | The page the loopback callback answers the browser with is reference-authored prose that `NOTICE` forbids shipping. This port serves its own success and failure pages at the same moments and with the same status, and the replay requires their digests to differ from the reference's | `SUCCESS_PAGE` and `FAILURE_PAGE` in `crates/vibe-core/src/auth/mcp_oauth/callback.rs`, and `is_page` in `crates/vibe-app-server/tests/mcp_oauth_parity_tests.rs` |
| A call header comes from the effect kind, not from a per-tool UI class | The reference builds a call display from the tool's own `ToolUIData` subclass (`vibe/core/tools/ui.py`), which falls back to `get_no_args_display` when the arguments never arrived and to `get_invalid_args_display` when they arrived in the wrong class; both publish the tool's display name alone over the running verbs. This port keys presentation on `ToolEffectKind`, this port's name-based vocabulary of the reference's fourteen kinds, which the PRD keeps as a non-goal to rework, so a call whose arguments are unusable keeps the kind's header and the kind's verbs instead of degrading to the bare name. Measured over the presentation corpus at the pin: the three header fields of the no-arguments case on 12 builtins, its two verbs on the 8 whose kind names a verb of its own, the same two verbs on the wrong-class case, the three tools whose invalid-argument display is the tool name in the clear (`task` as `subagent`, `web_fetch`, `web_search`), and the two call verbs of `exit_plan_mode`. Nothing is lost by it: the header this port publishes names the same tool and states more about the call than the name alone does | `ledger` in `crates/vibe-core/src/events/detail/detail_presentation_parity_tests.rs`, which fails both when an entry is added without a reason and when one of those fields stops diverging |
| A settled call repeats its subject instead of reading the result | The reference settles a call through `ToolUIDataAdapter.get_result_display` (`vibe/core/tools/ui.py`), which reads the returned payload: an error becomes the message and no verb is published, a skipped call reports the default skip label, and `exit_plan_mode` and `task` report what their result carried. This port settles from the same effect kind that produced the call header, and carries the error and the skip reason on the tool entry that holds them, so the settled line keeps the call subject. Measured: the error message on 12 builtins, the dropped verb on 14 tools with both remote stubs included, the skip label on 12, and the two tools whose successful result is settled from its payload upstream. Where the reference's settled sentence is authored text, `NOTICE` bars its wording as well, so the field would still not be reachable by settling from the result. The same holds for the head of a `ToolError` whose `model_detail` reaches the model alone (`vibe/core/tools/base.py:103-107`): upstream the head is the settled message through `error_display` (`vibe/app_server/_tool_projection.py:247`), and here the settled line keeps the subject, so the split has no field to fill | `ledger` in `crates/vibe-core/src/events/detail/detail_presentation_parity_tests.rs`, one entry per field per case, checked for staleness on every run |
| No call display carries a content preview | The reference's edit and write displays publish a `content` field holding a preview of what the call would change (`vibe/core/tools/ui.py`), which its terminal client renders under the header. This port publishes null there on every tool: the diff and the written body already reach the client on the tool entry, so filling the field would send the same bytes twice on every call. Measured on the two builtins that publish it upstream, `edit` and `write_file`, over the valid call and the eight path shapes | `ledger` in `crates/vibe-core/src/events/detail/detail_presentation_parity_tests.rs`, which fails if either tool ever starts publishing one |
| The label an argument of the wrong class is answered with | `NOTICE` forbids shipping upstream prose, and the label `get_invalid_args_display` publishes is authored text: one 17-character sentence shared by nine builtins and both remote stubs, and one of its own for `task`. This port answers that case with its own header, so the digest cannot match and never will. The corpus records the reference's label as a length plus a SHA-256 and never as text, which is what lets the divergence be measured without carrying the sentence | `ledger` in `crates/vibe-core/src/events/detail/detail_presentation_parity_tests.rs`, scoped to the three header fields of the wrong-class case |
| Authored call-display text in the plan and fetch tools | Two tools publish call displays whose sentences the reference authored: `exit_plan_mode` writes the summary and the message of a plan review and names the wait it opens, and `web_fetch` renders the target together with its timeout and names the fetch. This port writes its own for all four, covering the same facts against the same arguments, and reaching those digests would mean copying the reference's wording. Everything else these two tools publish compares as it stands, which is why the entries are scoped to those fields rather than to the tools | `ledger` in `crates/vibe-core/src/events/detail/detail_presentation_parity_tests.rs`, scoped to `summary`, `message`, `settledMessage` and `statusText` on the two tools |
| The process working directory is never changed | The reference can be standing inside the directory it is about to delete, because its own launch path moves the interpreter with `os.chdir`, so it steps back to the repository root before removing the worktree and falls back to it when the directory has already gone, since v2.24.2 through `PreparedWorktree.leave_if_current_directory` (`vibe/core/git/worktree/repository.py:165-172`), called just before the removal (`vibe/cli/entrypoint.py:339-340`). Nothing here can be in that position: a launch carries its effective directory as a value on `Arguments`, a session carries it as a field on the start parameters, and `set_current_dir` has no call anywhere under `crates/`, so the guard is unnecessary rather than missing. Porting it would mean writing the hazard first | `no_crate_changes_the_process_working_directory` in `crates/vibe-core/src/worktree/worktree_tests.rs`, which scans every `.rs` file under `crates/` and fails the moment one moves the process, so the divergence cannot stop being true without taking this row with it; the removal it protects is `remove_worktree` in `crates/vibe-core/src/worktree.rs`, which takes the worktree it removes as an argument and reads no ambient directory |
| The worktree lifecycle lines are plain text | Every line the reference's worktree block prints goes through `rich`, and all thirteen carry markup of their own: the three preparation and failure lines wrap their sentences in `[dim]` or `[red]` (`vibe/cli/entrypoint.py:228-242`), and the ten cleanup and prompt lines wrap theirs in `[yellow]` or `[dim]` (`:255-349`). Only the two questions themselves are written straight to standard error unwrapped. This port writes the same sentences, in the same order, on the same streams, with names quoted as Python's `repr` quotes them, as plain text. The lifecycle runs before the TUI is up, where a markup renderer is a dependency the launch does not otherwise need, and what an operator reads is the sentence rather than its color. The precedent is the onboarding rows above, which record the same rendering-layer divergence on the same terms | `preparation_narrates_on_standard_error_in_the_reference_order` and `a_preparation_failure_is_reported_on_standard_output_and_exits_one` in `crates/vibe-cli/tests/worktree_lifecycle.rs`, which run the real binary and assert the exact plain lines and the stream each lands on, plus `cleanup_names_the_worktree_before_removing_it` and `cleanup_keeps_a_worktree_another_session_holds` in `crates/vibe-cli/src/tui/startup/worktree.rs` for the removal lines |
| The worktree naming prompt is this port's own prose | An automatic worktree is named by asking a small model, and the instruction it is asked with is reference-authored text `NOTICE` forbids shipping. This port asks with an instruction of its own covering the same directives (a short lowercase hyphenated name drawn from the task, nothing else), through the same model choice, one attempt at 1.5 seconds inside a 2 second budget, 24 tokens, no retry. Everything around the answer is measured: the answer goes through the same slug rule, a missing or unusable one falls back to the prompt and then to a random slug, and the `autoName`, `slug` and `prepareAuto` families replay every step of that walk. Two models given different wording can still pick different names for the same prompt | `UtilityPrompt::WorktreeName` in `crates/vibe-core/src/prompt/library.rs` over `crates/vibe-core/src/prompt/assets/worktree_name.md`, sent by `suggest_worktree_name` in `crates/vibe-core/src/worktree/naming_model.rs` |
| A shell argument is refused at the schema boundary | Both implementations refuse the same arguments and only the kind differs: the reference refuses inside the handler and raises its tool error, this port refuses at the schema boundary before the handler runs. Moving the refusal later would trade a typed contract for a matching error kind, and the typed contract is the stronger one, so the difference is decided rather than left open | `WHY_ERROR_KIND` in `crates/vibe-core/src/tools/shell/session_parity_ledger.rs` holds the 7 entries, and `a_ledger_entry_whose_divergence_is_fixed_fails_the_suite` fails the day the reference stops raising from the handler |
| The managed shell's own action sentences | `NOTICE` forbids shipping upstream prose, so the sentence the reference publishes for each managed shell action has no byte-identical counterpart here. The corpus records a digest and a length instead of the text, and this port answers its own wording for the same action | `WHY_MESSAGE` in `crates/vibe-core/src/tools/shell/session_parity_ledger.rs` holds the 6 typed entries and `WHY_RENDERED` the 6 rendered documents that read each sentence back, the `described` digests in `crates/vibe-core/tests/shell-session/corpus.json` carry the reference wording, and `a_ledger_entry_whose_divergence_is_fixed_fails_the_suite` fails if one of them stops diverging |
| A shell approval names scope-invalidating syntax in this port's words | `NOTICE` forbids shipping upstream prose. When a heredoc, a file redirect, a parse error or a dynamic word stops the extracted parts from describing what runs, both implementations raise the same literal whole-command requirement with the same patterns; only its label differs, because the reference prints its own approval wording and this port names the same causes in its own | `UNSCOPED_LABEL` in `crates/vibe-core/src/shell/shell_parity_tests.rs` holds the 16 label fields, `committed_label` in `scripts/parity/shell_policy.py` commits the reference label as a digest, and `every_resolution_matches_the_reference` fails when one of them stops diverging |
| A PowerShell operand the Windows path parser rejects is asked about | A name such as `Env:\SECRET` is neither a drive path nor a provider path. The reference joins it to the cwd and grants it; the Windows path policy here cannot position a component that carries a colon, and an operand it cannot position is treated as outside rather than inside. Granting what the policy cannot read would be the weaker default, so the stricter answer is kept | `escaping_glob` in `crates/vibe-core/src/shell.rs`, and `an_operand_the_path_parser_rejects_is_asked_about` in `crates/vibe-core/src/shell/windows/windows_tests.rs` |
| Shell output decodes a narrower codec set | Reference `decode_console_safe` (`vibe/utils/io.py`) tries the byte-order mark, UTF-8, the Windows OEM code page or the locale codec, and then `charset-normalizer`, a statistical detector. This port reads a byte-order mark, recognizes unmarked UTF-16 by its NUL pattern, the one other encoding a Windows shell produces, and otherwise decodes UTF-8 with replacement. A statistical guess would make the same bytes decode differently as a stream grows, which a pollable log cannot take back | `decode_output` in `crates/vibe-core/src/tools/shell/decode.rs`, and `utf16_console_output_is_decoded_rather_than_interleaved_with_nuls` in `crates/vibe-core/src/tools/shell/tests.rs` |
| Nine arguments only this port declares | `--allowed-tool`, `--provider-style`, `--model`, `--input-price`, `--output-price`, `--api-base`, `--credential-environment`, `--session-root` and `--fake-response` exist here and nowhere upstream, where each is an unrecognized argument. They choose what the reference reads from its configuration (the provider, the model, the prices, the endpoint, the credential variable), where sessions are stored, and the scripted answer the composite action's self-test and the integration tests run against. All nine are hidden from the help, and each answers to its exact spelling only: an abbreviation never reaches one, so `--p`, `--o` and `--se` still resolve to `--prompt`, `--output` and `--setup`, and no ambiguity the reference reports grows a name it does not list | `PORT_ONLY_ARGUMENTS` in `crates/vibe-cli/src/argv.rs`, `a_port_only_argument_takes_no_abbreviation` in `crates/vibe-cli/src/argv/reading/reading_tests.rs`, and the nine `/extraArgument` entries in `LEDGER` in `crates/vibe-cli/src/cli_surface_parity_tests.rs` |
| The CLI help's descriptions are this port's own | `NOTICE` forbids shipping the reference's option descriptions and epilog sentences, so both help renders carry this repository's sentences under the same arguments, in the same order, with the same epilog headings and entry names; the replay compares those and records the prose once for `vibe --help` and once for the last line of `vibe mcp add -h` | `EPILOG` in `crates/vibe-cli/src/lib.rs` and the `/help/prose` and `mcp-add-help` entries in `LEDGER` in `crates/vibe-cli/src/cli_surface_parity_tests.rs` |
| A request for the Unified Harness always lands on the legacy one | The reference wheel carries its Unified Harness, so `--experimental-harness`, `--smart-approve` and the `vibe_cli_unified_harness_rollout` experiment select it there. This port is the legacy harness only (row 36), so it takes the path the reference itself takes when that backend cannot be created: the session runs on the legacy harness, `config/read` answers `harnessSelectionSource` `flag` with a startup issue filed under `--experimental-harness`, and the TUI shows that issue as it comes up, in this port's own sentence. The rollout never selects, as it does not upstream without the backend. Smart approve's classifier is a Unified Harness gate, so a `--smart-approve` session prompts for tool calls the ordinary way, which is what the reference's own comment says a legacy session would do | `HarnessSelection` in `crates/vibe-app-server/src/harness.rs`, `the_unified_request_falls_back_with_an_issue_on_the_flag` in `crates/vibe-app-server/src/harness/harness_tests.rs`, and `config_read_reports_the_harness_fallback_a_launch_resolved` in `crates/vibe-app-server/src/server/server_tests/wire_tests.rs` |
| Integer budgets are 64-bit and read in ASCII digits | `--max-turns` and `--max-tokens` are `type=int` upstream, and Python's `int()` is unbounded and reads any Unicode decimal digit. Here they are `i64`: a value past that range saturates, which keeps the budget it sets (never reached, or spent already) and loses only digits no budget distinguishes, and only ASCII digits are read. Whitespace, a sign and single underscores between digits are accepted as `int()` accepts them | `python_int` in `crates/vibe-cli/src/argv.rs` and `budgets_convert_as_python_converts_them` in `crates/vibe-cli/src/argv/argv_tests.rs` |
| The Unified Harness PTY helper is not an entry point | The reference entry point runs the Unified Harness's POSIX PTY helper when its first argument is `--internal-posix-pty-helper`, before any parsing (`vibe/cli/entrypoint.py:5-10`), because that harness spawns the same executable to host a terminal. Nothing here spawns it, so the argument is refused as any undeclared one is | `the_unified_harness_pty_helper_is_not_an_entry_point_here` in `crates/vibe-cli/src/argv/argv_tests.rs` |
| The process keeps its executable's name | The reference renames its process from the Python interpreter's name to `Vibe CLI` so it can be told apart in a process list (`vibe/cli/entrypoint.py:375-387`). This port is a native executable already named `vibe`, which is what a process list shows, and renaming it would take the unsafe calls the workspace lints forbid | `PUBLISHED_BINARIES` in `crates/vibe-cli/src/distribution/release_parity_tests.rs` |
